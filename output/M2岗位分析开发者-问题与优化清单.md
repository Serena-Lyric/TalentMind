# M2 当前回包复核补充（2026-08-30）

- 新回包实际 `job_definition=470`、`job_skill=470`，交接说明写 488，数量不一致；
- `job_name` 470/470 可以互相精确关联；
- `job_name_zh` 0/470；
- 相对于当前 285 条 canonical 词典，回包包含约 3158 个未对齐技能名（按唯一名统计）；
- 回包代码已迁入 `backend/app/job_analysis/`，硬编码密钥已清除；
- 旧 `input/jd-filter-package/` 已删除，正式系统不再依赖它。

以下清单中的 canonical、中文字段、数量差异和测试语义问题仍是待 M2 处理项。

# M2 岗位分析开发者问题与优化清单

- 审计日期：2026-08-30
- 上游输入：M1 `jd_pool`
- 下游：M3 图谱、M4 匹配、A API、M5 前端

## P0 必须处理

### M2-P0-01 修复 job_skill 与 job_definition 关联

当前 `exchange/m2/job_definition.json` 有 22 条，`job_skill.json` 有 22 条，但岗位名仍有 19/22 无法精确关联。例：岗位定义使用英文 key，技能明细使用中文岗位名。

要求：

- `job_skill.job_name` 必须等于 `job_definition.job_name`；
- 推荐增加稳定 `job_id`，但不能用易变的中文展示名作为关联 key；
- `validate_exchange` 必须 0 关联警告；
- 重新导出 job_change_log 时也必须使用稳定关联。

### M2-P0-02 主产出增加 `job_name_zh`

不要继续依赖 A 按两个 JSON 数组顺序 zip 的过渡方案。主 `job_definition.json` 每条记录增加：

```json
{"job_name":"Senior Cloud Solutions Architect","job_name_zh":"高级云解决方案架构师"}
```

技能 canonical 不翻译，技术技能保持英文 canonical，软技能按词典 canonical 保持中文。

### M2-P0-03 交接产出带版本头

建议所有交接 JSON 统一采用对象根节点：

```json
{
  "schema_version":"1.0",
  "contract_version":"2026-08-03",
  "generated_at":"2026-08-30T...",
  "module":"m2",
  "items":[...]
}
```

如果暂时必须保持数组根节点，应在 README 中提供等价版本头，并让校验器检查。

## P1 重要优化

- `is_emerging` 当前 22 条全 true，应提供人工复核状态和依据，不能将“首次出现”直接等于“新兴岗位”；
- `evolution.stage` 的英文枚举需在契约中固定，前端只做展示映射；
- `source` 应按当前 M1 真实来源更新，不要继续保留旧 `dataset` 语义；
- job_skill 的 `evidence` 应可回溯到 JD 标识，且 `confidence`、`weight` 范围要校验；
- 产出至少 100 条 JD 标注集和抽样准确率报告，避免只有 22 个最终岗位而无法证明解析质量；
- skill_dict 未命中项要进入人工复核，不要静默丢弃后让下游误以为技能不存在。

## 回包验收

- [ ] 22/22 job_skill 可关联；
- [ ] 22/22 主岗位包含 job_name_zh；
- [ ] 所有技能属于 285 条 canonical 或有明确复核清单；
- [ ] validate_exchange 通过；
- [ ] M3 可以基于新包生成图谱；
- [ ] 不再需要 `job_definition_zh.json` 作为主展示名来源。

## 2026-08-30 验收证据

- 回包文件实际：`job_definition=470`、`job_skill=470`，不是交接说明中的 488；
- 470 条 `job_name` 已 1:1 对齐；
- `job_name_zh=0/470`；
- 当前校验器结构校验通过，但 canonical 警告 4508 条；
- M2 管道源码已迁入正式包并修正仓库路径；正式配置不再携带回包中的硬编码密钥；
- `tests/test_job_analysis` 当前 53 passed、5 failed，失败项保留为模块/旧测试语义问题，未在本次迁移中擅自改逻辑。
