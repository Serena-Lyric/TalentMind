# TalentMind 项目笔记 —— 全队通知与开发须知

> 日期：2026-08-13（整合阶段 0–6 完成、数据闭环打通）；2026-08-14（MVP 复查：2 项修复 + 2 项契约提醒，见第九节）
> 用途：A 同学向全队发布通知；各模块队员二次开发前必读。
> **记录原则**：只记录**对应模块开发者会遇到或需要处理**的事项（记入对应模块节）；与任何模块都无关的工程问题归档于 `docs/superpowers/资产与状态.md`（工程经验）。
> 权威文档：`docs/superpowers/决策跟踪.md`（决策 D1–D36）、`docs/superpowers/资产与状态.md`（资产/状态/已知限制/工程经验）、`docs/superpowers/specs/2026-08-03-team-plan-design.md`（契约）、`docs/superpowers/specs/2026-08-11-repository-organization-design.md`（目录边界）。

---

## 〇、全队通用通知（所有成员必读，A 可直接转发）

1. **统一响应格式**：所有接口返回 `{code, message, data}`，**`code=0` 表示成功**（此前前端文档/图谱曾用 200，已统一为 0）。错误时 HTTP 仍为 200，业务错误经 `code` 表达。
2. **数据契约冻结**：`backend/app/contracts/ddl.sql`（8 表）。**加表/加字段自由；改/删字段必须先通知全队**。
3. **技能对齐（重要）**：所有模块的技能名必须对齐 `backend/app/skills/skill_dict_seed.json`（**285 个 canonical**）。M2 已接入约束抽取；M3/M4 输出均 canonical 化。技能不在 seed 时应先补 seed（别名）或放入人工复核，禁止自由命名。
4. **`job_change_log.change_type` 扩展枚举**（D32）：除 `added/removed/modified` 外，M2 会输出 `duties_changed / scenarios_added / scenarios_removed / evolution_changed`，已写入 DDL 注释，消费方需兼容。
5. **`jd_pool.experience` 字段**：已扩容 `VARCHAR(255)`（D33，2026-08-15 执行，通知随全队会议）；`repository.py` 已移除 `[:32]` 截断；2026-08-16 cleaner 提取上限 255（修复单行描述捕获整段致 1406，见 traps/2026-08-16-cleaner-experience-overlength.md）。
6. **接口基线**：以**前端 20 个接口**为准（`frontend/前后端接口对接文档.md`），后端已全部实现；**前端 mock 已全部删除**，页面直连真实后端。
7. **目录边界**：正式后端源码只放 `backend/app/`，前端只放 `frontend/`；交接文件放 `exchange/m1~m5`（**小写**）；大型数据放 Git 忽略的 `data/local/`。根目录不再允许新增交付目录。
8. **LLM 配置**：M2 管道读取 `backend/.env` 中的 `LLM_API_KEY / LLM_BASE_URL`（DeepSeek 兼容格式），`.env` 不入库。
9. **测试命令**：后端测试统一 `cd backend && .\.venv\Scripts\python.exe -m pytest`（裸 `pytest` 会报找不到 `app` 模块）。
10. **工作前必读**：`docs/superpowers/资产与状态.md`（资产位置、已知限制、工程经验、维护规则）。
11. **`job_change_log.change_type` 列宽（2026-08-14 发现，2026-08-15 已扩容）**：DDL 原为 `VARCHAR(16)`，D32 扩展枚举 `scenarios_removed` / `evolution_changed` 长度 17，MySQL 严格模式实测报 `ERROR 1406`；已扩容 `VARCHAR(32)`（P4，正式通知随全队会议）。
12. **测试数据自动清理（2026-08-16，D37）**：任何写库测试必须按测试夹具特征精确清理（finally/teardown），禁止按 source 等宽泛条件 DELETE；跑完集成测试后必须查询 DB 确认无测试残留（曾因 `DELETE WHERE source='dataset'` 误删 jd_pool 5000 条，见 `docs/superpowers/traps/2026-08-16-integration-test-wiped-jd-pool.md`）。
13. **M1 来源标签（2026-08-17，D38）**：`jd_pool.source` 由 `'dataset'` 改为 `'linkedin'`（来源平台，D17；不伪造多源），新增 `source_detail`（posting_domain / 数据集标识 `linkedin_job_postings`）；M2 下次重跑后 `job_definition.source` 将变为 `["linkedin"]`。多源采集（中文平台）计划中，届时 source 扩展多平台数组。

---

## 一、M1 数据采集（A 负责）

**现状**：`backend/app/collect/` 可运行；`jd_pool` **126133 条 cleaned**（linkedin 123849 + **hn 1796** + **boss 488**；D38/D40/D45/D48/D49）；postings.csv archive 已于 2026-08-20 全量导入，先核对 5000 行，再以 offset=5000 续导 118849 行；**source='linkedin'/'hn'/'boss' + source_detail**；`talent_raw` 0；`signal` 表 **433 条**（source=github/blog，4 天时序）；BOSS 使用 9333 端口完成扩大采集（8 关键词×3 城市×5 页，listed=415/details=300/new=319/skipped=96），原有 151 条全部保留；用户选择“我要投职”求职端，低速循环已完成 15 轮并新增 14 条，第 16 轮因 CDP 无可用页面安全停止；当前独立 Edge 待人工注销。

**通知/须知**：
- 已满足 ≥100 条（jd_pool 126133 条 cleaned，2026-08-20）；archive `postings.csv` 共 123849 条有效岗位，导入器支持 `--offset/--limit/--batch-size` 流式断点续导；Git 忽略。
- `experience VARCHAR(32)` 截断问题见全队通知第 5 条（D33 后续扩容，先别改契约）。
- 运行测试必须 `python -m pytest`（见全队通知第 9 条）；venv 需按 `requirements.txt` 完整安装（含 `beautifulsoup4` 等）。
- 真实爬虫（BOSS/猎聘等）可行性参考 `docs/superpowers/archive/2026-08-20/legacy/招聘网站爬虫方案.md`（2026-06-22 调研，**时效需复核**）；`signal` 为 P1 不阻塞。
- 交付物已产出（2026-08-16 重新导出）：`exchange/m1/jd.json`（200 条）、`quality_check.md`（10 条抽样待 A 补结论）、`接口自述.md`。
- 中文平台：BOSS 已按 D45–D48 落地为用户人工登录、Edge CDP 受控采集；拉勾/猎聘/智联仍按 P6 暂不抓取，不绕过 robots/反爬。

---

## 二、M2 岗位分析

**现状**：已迁入 `backend/app/job_analysis/`（7 步管道）；**2026-08-13 已用约束管道重跑：424 条 JD → 22 个岗位定义**（成本约 $0.94 / 560 次调用）。

**通知/须知**：
- **stage3 已接入 skill_dict 约束**：LLM 只能输出 seed 内 canonical，无法映射的技能进 `unknown_skills`/人工复核，不再自由提取（反幻觉机制）。
- **运行方式**：`cd backend && .\.venv\Scripts\python.exe -m app.job_analysis.main <输入SQL路径>`；需要 `LLM_API_KEY`（backend/.env）。
- **输入数据**：原 421MB `seed_jd_pool.sql` 已删除（D27）；压缩备份在 `docs/submit/seed_jd_pool.sql.gz`（119MB）。M2 输入应来自 M1 导出的 jd_pool SQL/JSON。
- **输出**：`exchange/m2/`（7 个 JSON，小写目录）。产出由 A 的 `import_exchange.py` **全量重建**导入 MySQL（M2 产出是权威岗位集合）。
- **断点续跑**：中间结果在 `backend/app/job_analysis/data/`（Git 忽略）；重跑会覆盖 `exchange/m2` 产出。
- **二次开发注意（含整合教训）**：
  - 新增技能先补 `skill_dict_seed.json` 再跑管道（曾遇 `敏捷/看板` 未命中 seed，已补别名）。
  - 旧逻辑产出（未过约束）技能与 seed 不对齐，**必须用约束管道重跑**后才能被 M3 正常消费。
  - `change_type` 扩展值见全队通知 4；阈值（质量线 0.65 / 拒绝线 0.5）在 `config.py` 可调。
  - 测试集与准确率报告（P1）待做。

---

## 三、M3 图谱

**现状**：`backend/app/graph/builder.py`；基于 M2 产出构建 **97 节点（22 岗位 + 75 技能）/ 222 边**；已导入 Neo4j（Job 22 / Skill 75 / REQUIRES 191 / RELATED_TO 31）。

**通知/须知**：
- **节点 id 契约**：Job 节点 id = `job_name`，Skill 节点 id = `skill_dict.canonical`。
- **技能归一**：未命中 seed 的技能会被自动过滤（D31）；若图缺技能，先检查 M2 产出/seed。
- **数据源**：默认读 `exchange/m2/job_definition.json`；mock 数据仅作回退/演示，**正式图必须来自 M2 产出**。
- **导入 Neo4j**：`import_graph.py` 用 Cypher MERGE（幂等）；**换数据源后建议先清空 Neo4j 再导入**，避免新旧图混入。
- **二次开发注意（含整合教训）**：
  - `backend/tests/test_graph.py` 的输出**已隔离到临时目录**（monkeypatch OUTPUT_PATH）——测试构建不得覆盖正式 `exchange/m3/graph.json`（曾因此污染正式产出与 Neo4j）。
  - 节点 id 曾用序号（job_1/skill_1），已改为 name/canonical；消费方注意。
  - `RELATED_TO` 基于技能 Jaccard 相似度（阈值 0.25），可增强（P1）；**industry 节点缺失**待补（前端图谱需要）。
  - 图谱可视化 HTML（vis-network）原型在 `docs/prototypes/`。

---

## 四、M4 简历匹配

**现状**：`backend/app/matching/`；API `POST /api/resume/upload` 支持 **PDF / DOCX / DOC / TXT**（依赖已装，安装即用）；输出技能 canonical 化；匹配基于加权规则。

**通知/须知**：
- **文件解析依赖**：`pdfplumber / python-docx / mammoth` 已加入 `backend/requirements.txt`（`pip install -r requirements.txt` 一次装齐）。
- **上传接口**：`POST /api/resume/upload`（FormData `file` 或表单 `content` 文本）；返回 `{profile, matchResult}`，`matchResult` 含 `score / matched / missing / strengths / target_job`。
- **真实简历不提交**（D36）：测试用生成/脱敏文本（`backend/tests/fixtures/matching/`）。
- **二次开发注意（含整合教训）**：
  - `extract_skills` 对"中文紧邻英文"（如 `熟悉Python`）提取失效（原型 `\b` 边界问题），文本解析需正则/LLM 补充处理。
  - 差距分析/学习路径（`pathfinder`）**尚未实现**；匹配为加权规则（编程语言 1.5x / 后端框架 1.3x / 数据库 1.2x），**非 embedding**。
  - 输出契约不含 status；不编造时间/数字（DR-2）。
  - 测试集（≥30 份简历标注）与准确率报告（P1）待做。

---

## 五、M5 前端

**现状**：`frontend/`（Vue3 + TS + Vite + Element Plus + ECharts + G6）；6 页；20 接口全部对接真实后端；**mock 已全部删除**。

**通知/须知**：
- **接口层**：`src/utils/request.ts` 统一解包 `{code,message,data}`（code=0 成功）；文件流接口（`/api/jobs/export`）不走统一响应体。
- **开发联调**：`vite.config.ts` 已配置 `/api` 代理到 `http://127.0.0.1:8000`——**本地 `pnpm dev` 直接可用，无需环境变量**；后端 CORS 已开。
- **构建**：`pnpm install`（首次需 `pnpm approve-builds --all` 放行 core-js/esbuild/vue-demi）；`pnpm run build` 通过。
- **页面数据状态**：数据概览（岗位总数/技能分布真实，趋势/雷达占位）；岗位管理（22 条真实，CRUD/导入导出可用）；图谱（97 节点真实）；简历（真实上传解析+匹配）；学习路径/简历预览（**真实空态**，待数据源）。
- **二次开发注意（含整合教训）**：
  - **类型检查已接入（2026-08-14 修复）**：build 已改为 `vue-tsc --noEmit && vite build`（typescript 锁 `~5.3.3`）；import 缺失等错误不再漏过。
  - 清理 mock 分支时曾误删 api 文件的 request import（`get/post/put/del`），删代码注意保留 import。
  - 岗位数据无 `evolution` 字段（后端未产出），表格演进列已用 `?.` 兼容（显示 0/无）。
  - 接口字段以 `frontend/前后端接口对接文档.md` 为准；页面新增功能必须走 API，**禁止再引入本地 mock 数据文件**。
  - `package.json` 曾带 BOM 导致 pnpm 拒绝，注意编码。

---

## 六、工程问题归档（与各模块职责无关，供 A/本地运维参考）

整合过程遇到的纯工程问题（路由注册顺序、`python-multipart` 依赖、router 双重前缀、`import_exchange` 全量重建语义、进程端口清理等）**不放入本笔记**，归档于 `docs/superpowers/资产与状态.md`（工程经验小节）。模块开发者一般不会遇到，若遇到请先查上述文档。

---

## 七、待办清单（供 A 跟踪）

1. **M1**：jd_pool 补足 ≥100 条；导出 `exchange/m1/jd.json`、质量核对记录。
2. **M2**：≥100 条 JD 标注测试集与准确率报告（P1）；多批次后补趋势数据。
3. **M3**：industry 节点补充（前端图谱需要）；RELATED_TO 增强（P1）。
4. **M4**：pathfinder 差距分析/学习路径；PDF/DOCX 解析的单测（当前为文本测试）；≥30 份简历标注测试集（P1）。
5. **M5**：趋势/雷达真实数据接入；Learning/ResumeDemo 数据源；城市/薪资等字段映射（类型检查已接入，2026-08-14）。
6. **A 集成**：`experience` 扩容（D33，执行前通知全队）；部署说明与演示材料（P3）；阶段 7 清理（根目录原交付目录，`人岗匹配/` 真实简历删除前确认）。
7. **契约**：改/删字段前通知全队（D32/D33 已在通知范围）。
8. **M2**：`job_skill.json` 的 `job_name`（中文译名）与 `job_definition.json`（英文）**19/22 无法关联**（3/22 精确匹配，2026-08-16 复核），需输出稳定关联字段（如 `job_name_en` / 稳定 `job_id`）后再由 A 重导；否则 job_skill 证据链无法回溯到岗位定义。
9. **A 集成 / 契约**：`change_type` 扩容 `VARCHAR(32)`（D32 枚举超长，执行前通知全队，同 D33 流程）；简历上传结果未写入 `resume` 表（dashboard `totalResumes` 恒 0，待定是否落库）。
10. **仓库**：根目录 `笔记.md` 不提交（用户要求，见 A_AGENT_HANDOVER 红线），作为工作树文档使用。

---

## 八、字段偏差对照与二次开发修正依据（2026-08-13）

> 对照基准：`docs/superpowers/specs/2026-08-03-team-plan-design.md`（§6 数据契约 / §7 图谱 / §8 交接文件 / §9 接口）+ `backend/app/contracts/ddl.sql` + `plans/2026-08-14-module-roundtrip.md`（原 08-08 planM* 已归档 plans/archive/）；实际来源：M2 约束重跑产出（22 岗位）、M3 graph.json、后端 `/api` 返回、前端接口层。
> 说明：以下"偏差"多为**契约未定义/未对齐**，非错误；加字段自由（D11/D23），但消费方与展示层需明确口径。

### 8.1 M2 岗位分析

| # | 位置 | 设计/契约要求 | 模块实际 | 影响 | 二次开发修正建议 |
|---|---|---|---|---|---|
| M2-1 | job_definition.json | 字段见 DDL + §8 示例（无 source_jd_count） | 多出 `source_jd_count`（该岗位合并的 JD 数） | 消费方可忽略 | 契约说明补充该字段（加字段自由） |
| M2-2 | evolution.stage | D13 中文阶段：萌芽/增长/成熟/衰退 | 英文枚举 `emerging/growth/mature/declining` | 前端展示需翻译 | 前端映射表，或 M2 输出中文枚举（待定） |
| M2-3 | job_skill 条目 | §6.3：skill_id/name/weight/confidence/evidence/is_required（6 字段） | 多出 `evidence_jd_count`（7 字段） | 可忽略 | 契约说明补充 |
| M2-4 | job_skill 顶层 | DDL：jd_id/job_name/level/skills/duties/extracted_at | 仅 job_name/skills；jd_id/level/duties 缺失 | 导入时 duties 为空、jd_id/level 为 NULL；无法回溯到原始 JD | M2 尽量带 jd_id（或 A 导入时从 job_skill 证据反查） |
| M2-5 | job_name 语言 | §8 示例为中文（如 RAG工程师） | 主产出 `job_definition.json` 为英文名，中文在 `job_definition_zh.json` | 前端/图谱/API 显示英文岗位名 | 确定展示语言策略：用 zh 文件作为展示源（A 集成可切换） |
| M2-6 | is_emerging | D12：聚类+LLM+人工确认 | 当前 22 条全部 true（首次无存量 diff，新岗位均标记） | 新岗位标记失真 | 建立人工复核流程（manual_review 已产出）；后续批次 diff 后修正 |

### 8.2 M3 图谱

| # | 位置 | 设计/契约要求 | 模块实际 | 影响 | 二次开发修正建议 |
|---|---|---|---|---|---|
| M3-1 | RELATED_TO | §7"技能关联"，表述含糊；planM3 P1 可增强 | 实现为**岗位-岗位相似度**（Jaccard 阈值 0.25） | 图谱岗位间连线（非技能关联） | 确认语义：保留 job-job 并文档说明，或补 Skill-Skill 关联（P1） |
| M3-2 | industry | 前端 GraphNode 期望 kind=industry | 节点 `industry=""`，无 industry 节点 | 图谱缺少行业层级 | M2 无行业字段；由集成层补充赛道映射或前端降级（仅 job/skill） |
| M3-3 | REQUIRES 标记 | §7"required/bonus 标记" | 用 `is_required` bool | 语义一致，命名不同 | 文档对齐即可 |

### 8.3 M4 简历匹配

| # | 位置 | 设计/契约要求 | 模块实际 | 影响 | 二次开发修正建议 |
|---|---|---|---|---|---|
| M4-1 | matchResult.path | planM4 输出 `{target_job,score,matched,missing,path}` | 无 `path`（差距路径建议未实现） | 前端学习路径页无法接真实数据 | M4 实现 pathfinder（不编造时间/数字，DR-2） |
| M4-2 | missing[].level/tip | 前端 MatchResult 期望 `{name,level,tip}` | 后端仅填 name，level="medium"、tip="" | 前端缺失技能显示无建议 | M4 按技能类别/证据生成 level 与 tip |
| M4-3 | strengths | 前端接口要求 | 返回 resume_extra_skills | 正常 | 保持 |

### 8.4 M5 前端 / A 集成 API

| # | 位置 | 设计/契约要求 | 模块实际 | 影响 | 二次开发修正建议 |
|---|---|---|---|---|---|
| M5-1 | /api/jobs 列表项 | 前端 Job 模型 15 字段（含 company/city/type/salary/track/kind/evolution/skillDetails/jdVersions） | company/city/type/salary/track/kind 为空；无 evolution/skillDetails/jdVersions | 岗位页大量字段空白 | 后端需 M1/M2 提供原始 JD 提取字段（公司/城市/薪资等 DDL 无对应列，需新增表/字段并通知全队）；前端先行隐藏空列 |
| M5-2 | evolution | 前端期望 `{added,removed,changed}` | 后端无（job_change_log 空，需 M2 多批次 diff） | 已用可选链显示 0/无 | 等 M2 第二批数据产生 change_log 后接入 |
| M5-3 | graph kind=industry | 前端期望 3 类节点 | 仅 job/skill | 图谱无行业层 | 同 M3-2 |
| M5-4 | dashboard trend/skill-radar/industry-tracks | 前端图表 | 返回空/占位 | 图表空态 | 等 M2 多批次历史数据；industry-tracks 可先静态赛道表 |
| M5-5 | status | D34：前端招聘状态（展示层） | 后端硬编码 "open" | 语义占位 | 后续从 JD 提取或前端移除该列 |
| M5-6 | 岗位名称语言 | — | API 返回英文 job_name | 页面英文 | 同 M2-5（切换 zh 源） |


---

## 九、MVP 整合复查记录（2026-08-14，A 已完成修复）

> 复查范围：设计文档 + 资产状态 + 后端全量测试（191 通过）+ 20 接口冒烟 + 前端构建（`vue-tsc --noEmit && vite build` 通过）。
> 结论：数据闭环可用（MySQL 22 岗位 / Neo4j 97 节点 222 边 / API 全通 / 前端类型检查通过）。

### 9.1 已修复（本次）

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| F1 | `backend/app/integration/import_exchange.py` | `import_change_logs` 向 `job_change_log` 插入 DDL **不存在的 `object_type` 列**；且 M2 产出的 `job_id` 是 job_name（字符串），DDL 语义是 job_definition.id。当前 change_log 为空所以从未触发，**M2 首批 diff 产出日志时导入必崩** | 去掉 object_type；job_name→job_definition.id 解析（解析不到跳过）；改为全量重建；新增集成测试 `test_import_change_logs_job_name_resolution`（集成测试 11 项通过） |
| F2 | 前端工具链 | `vue-tsc` 1.8.27 与 TypeScript 5.9.3 不兼容（`Search string not found: "/supportedTSExtensions/"`），类型检查一直跑不起来，`build` 也不做类型检查 | `package.json` 锁定 `typescript ~5.3.3`；`build` 改为 `vue-tsc --noEmit && vite build`；顺带修复 30 个既有类型错误 |

### 9.2 类型检查暴露并修复的运行时隐患（F2 附带）

- **Graph.vue**：`graph.fitView(20)` 参数不符合 G6 v5 API（应为 `{ when: 'always' }`），原代码靠 try/catch 吞错 → 图谱可能不自动适配视口；`graphStats` 读 `data.stats` 恒为 0（当前数据对象无 stats 字段）→ 改用 `graphData.value.stats`。
- **ResumeDiagnosis.vue**：`currentJob` 可为 undefined（目标岗位列表加载失败时），且 `dimensions/matched/missing` 为可选字段，模板直接取值会崩溃 → computed 增加兜底空结构，接口字段改必填。
- 其余为未使用导入/变量、隐式 `any` 等类型卫生问题（LearningRoadmap / Jobs / Dashboard / router），已清理。

### 9.3 需全队/契约处理（本次新发现，尚未修复）

| # | 位置 | 问题 | 影响 | 处理 |
|---|---|---|---|---|
| C1 | `backend/app/contracts/ddl.sql` | `job_change_log.change_type VARCHAR(16)` 装不下 D32 扩展枚举 `scenarios_removed` / `evolution_changed`（17 字符），MySQL 严格模式实测报 `ERROR 1406` | M2 首次产生这两类 diff 时导入即失败 | 扩容 `VARCHAR(32)`；**改字段须先通知全队（同 D33 流程）**，通知后再改 DDL 并同步本笔记 |
| C2 | `exchange/m2/job_skill.json` | `job_name` 为中文译名，与 `job_definition.json` 英文 `job_name` **19/22 不匹配**（3/22 精确匹配，2026-08-16 复核），job_skill 无法完整回溯到岗位定义 | 证据链断裂；job_skill 表与 job_definition 无法 join | M2 二次开发需输出稳定关联（如 `job_name_en` 双字段或稳定 job_id）；A 导入层暂存中文名，等待契约补齐后重导 |

### 9.4 复查确认的其他已知项（不新增动作）

- `resume` 表恒空：`POST /api/resume/upload` 只即时返回、不落库 → dashboard `totalResumes=0`（MVP 可接受，待定是否落库）。
- `笔记.md` 按用户要求不提交（A_AGENT_HANDOVER 红线），作为工作树文档使用。
- Learning 页 `jobPaths` 无任何数据源（真实空态），与“mock 已全部删除”一致。
- M2-1~M2-6 / M3-1~M3-3 / M4-1~M4-3 / M5-1~M5-6 字段偏差仍按第八节执行，不重复。


---

## 十、input 原版模块对比发现（2026-08-14，供全队会使用）

> `input/` 为队员提交的四个模块原版（M2 jd-filter-package / M3 图谱模块 / M4 人岗匹配 / M5 岗位能力图谱-前端源码）。已与整合版（backend/app/、frontend/）逐模块对比，结论如下。

### 10.1 中英文分裂的根源（M2 原版自带，非整合引入）

- 原版 `translate.py` 的 `translate_job_skills` 会把 `job_skill.json` 的 **job_name 和技能名都翻译成中文**；
- 原版产出实测：`job_definition.json` job_name=英文（Cloud Platform/Big Data Engineer）、技能含英文软技能（communication skills）；`job_skill.json` job_name=中文（云平台/大数据工程师）、技能=中文（沟通能力）→ **0/8 同名关联，两套技能词表**；整合版 22 条同样 0/22；
- **翻译技能名会破坏 skill_dict.canonical 全局唯一**（communication vs 沟通是两个节点），危害图谱与匹配。统一方案见 `docs/superpowers/plans/2026-08-14-remediation-plan.md` 第三节（英文 key + job_name_zh 展示；技能词表唯一、禁止翻译技能名）。

### 10.2 其他对比结论

| 模块 | 原版 | 整合版 | 结论 |
|---|---|---|---|
| M2 | stage3 自由提取、无约束 | skill_dict 约束（D31） | 整合正确，保留 |
| M3 | 数据源仅 mock；RELATED_TO=Job-Job（阈值 0.25） | M2 产出优先 + 技能归一 | RELATED_TO 语义来自原版，需裁决保留 |
| M4 | matcher 无 canonical；测试集 test_resumes/jobs/dataset.json 在 | 输出 canonical（D31）；测试集已迁 fixtures | 整合正确；pathfinder 原版也未实现（P0 待做） |
| M5 | LearningRoadmap 依赖 `src/mock/learning-data.js`（jobLearningPaths）；全站 mock | mock 删除、接真实 API；Learning 空态 | mock 方向正确，但 Learning 数据源需由后端 pathfinder 补上（B4） |
| 交付物 | exchange 大写 `M2`；无接口自述 | 小写 m1~m5（D26）；接口自述模板仍缺 | 补齐 plan0 遗留项 |

### 10.3 input/ 目录保护

- `input/人岗匹配/岗位测试用例/` 含**真实简历 PDF/DOCX**（D36 不提交）；
- 已加入 `.gitignore`（`input/` 整目录忽略），**禁止 `git add -A`**；原版代码仅作对比参考，正式源码以 `backend/app/`、`frontend/` 为准。


---

## 十一、全队会议材料：模块外发-回发闭环与要求清单（2026-08-14）

> 完整方案：`docs/superpowers/plans/2026-08-14-module-roundtrip.md`（五步闭环 + 回包结构 + 防覆盖机制）。

### 11.1 闭环流程（A 与各模块开发者共同遵守）

```
A 打通连接 → A 回发（修改版代码+问题清单+要求清单+数据快照+契约） → 队员二次开发
→ 队员回包（代码+交接产出+变更说明+测试结果） → A 验收集成（schema→diff→单测/集成→前端冒烟）
```

- **防覆盖**：A 修改过的文件清单随包写明，队员不得回滚；A 验收时 diff 检查。
- **数据快照**：A 会把集成库权威数据（M2 产出/skill_dict/graph.json）随包发出，队员本地联调不再 mock 空转。
- **版本号**：建议 M2 v3 / M3 v2 / M4 v2 / M5 v2 起（以当前整合版为基线），交接文件带版本头。

### 11.2 各模块要求清单（P0 先做）

| 模块 | P0 要求 | P1 要求 |
|---|---|---|
| M2 | ① translate.py 禁止翻译技能名；job_skill 用英文 key；job_definition 加 job_name_zh；② 产出带版本头 | is_emerging 人工复核；≥100 JD 测试集+准确率报告 |
| M3 | 确认 RELATED_TO=Job–Job 语义写入 README；节点支持 name_zh | Skill–Skill 关联增强 |
| M4 | pathfinder（missing→level/tip + 差距路径，不编造数字）；≥30 简历测试集+准确率报告 | 中文紧邻英文抽取；resume 落库 |
| M5 | 对接新 API（title 中文/name_en）；build（含 vue-tsc）通过 | 隐藏空列；Learning/ResumeDemo 接数据源 |

### 11.3 会议议程（A 主持）

1. 现状：MVP 已打通（22 岗位/97 节点/20 API/六页，191 测试 + 前端构建通过）
2. 契约基线 + 中英文统一规则（英文 key + job_name_zh；技能 canonical 唯一、禁止翻译技能名）
3. 交接规范（exchange 小写、版本头、schema 校验器）
4. 各模块问题与要求（11.2 逐模块过）
5. 回包规范与时间表（8/14–15 回发 → 8/16–19 开发 → 8/20–21 验收 → 8/22–24 端到端+演示）
6. 分工：部署说明/演示视频/准确率报告/六页演示
