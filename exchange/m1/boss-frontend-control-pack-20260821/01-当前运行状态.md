# 01 当前运行状态

## 核验时间

- 日期：2026-08-21
- 工作区：`D:\Application\ClaudeCode\repository\TalentMind`
- 数据库查询从 `backend` 目录执行，使用项目虚拟环境。

## BOSS 运行状态

| 项目 | 当前结果 | 说明 |
|---|---|---|
| 采集 Python 进程 | 未发现 BOSS 采集进程 | 本次使用 `--once`，完成后按设计退出；此前低速循环也已因 CDP 页面目标异常安全停止 |
| `127.0.0.1:9333` | 正在监听 | 监听进程为独立 Edge；端口可访问 |
| CDP `/json/version` | HTTP 200 | 返回 Edge CDP 协议信息 |
| CDP `/json` | HTTP 200 | 核验时至少有一个 `zhipin.com` 的 `job_detail` 页面目标 |
| BOSS 页面登录 | 可连接页面，但登录有效性仍以采集时页面实际状态为准 | 若出现登录/验证页，采集器应停止并提示人工处理 |
| 当前任务状态 | `idle` / `ready_to_start` | 可以再次启动单轮或持续循环，当前没有后台任务 |

## 本次单轮启动结果

执行时间：2026-08-21 09:50:10–09:54:37（执行记录）

```text
开始：2026-08-21 09:50:10
完成：2026-08-21 09:54:37
listed=12
details=8
new=0
skipped=12
```

解释：本轮列表识别 12 条岗位，补采详情 8 条；12 条 URL 均已存在，因此没有新增入库。单轮正常退出，不代表持续采集仍在运行。

## 数据库实时基线

| 指标 | 数值 |
|---|---:|
| `jd_pool` 总量 | 126,133 |
| `source=linkedin` | 123,849 |
| `source=hn` | 1,796 |
| `source=boss` | 488 |
| BOSS `status=cleaned` | 488 |
| BOSS `source_detail` 空值 | 0 |
| BOSS `source_detail` 重复数 | 0 |
| BOSS 占位/空 URL | 0 |
| BOSS `duties` 非空 | 243 |
| BOSS `MAX(crawled_at)` | 2026-08-20 12:20:44（数据库返回值） |
| `talent_raw` | 0 |
| `signal` 总量 | 485 |
| `signal` github/blog | 114 / 371 |

上述数量是核验时数据库结果，不是前端进度百分比。BOSS 采集没有预先知道的全量总数，前端不应把 `listed` 当作全站总量。

## 历史低速循环结果

现有日志 `data/local/logs/boss_collect_loop.out.log` 的最后一次正式低速运行：

- 第 1–15 轮完成；累计 `listed=180`、`details=120`、`new=14`、`skipped=166`。
- 第 16 轮在 2026-08-20 20:27:19 +08:00 因 CDP 无可用页面目标安全停止。
- 9333 端口当时仍监听，但“端口监听”不等于“采集进程正在运行”。

## 对前端的展示建议

当前页面建议同时展示三种状态，避免混淆：

1. **任务状态**：是否有采集器进程（idle/running/stopped/error）。
2. **浏览器连接状态**：9333 是否可连接、是否存在 BOSS 页面目标、是否疑似登录/验证页。
3. **数据状态**：本轮计数和数据库累计计数。


## 2026-08-22 当前运行复核

> 本节覆盖本文前面的 2026-08-21 历史快照；历史单轮记录和第 16 轮停止记录不改写。

- 核验时间：2026-08-22 11:25:46（Asia/Singapore）。
- BOSS 持续循环启动器 PID `33828`、工作进程 PID `26124` 均存活，启动时间为 2026-08-21 11:04:38；当前不是 `idle`，而是 `waiting_next_round`（第 116 轮已完成，等待下一关键词/城市）。
- 运行命令：`python -m app.collect.boss_collect_loop --cdp http://127.0.0.1:9333 --user-data-dir C:\Users\SERENA~1\AppData\Local\Temp\TalentMind-BOSS-Edge-9222 --forever`。
- 第 116 轮于 2026-08-22 11:25:20 完成：`listed=12`、`details=8`、`new=1`、`skipped=11`；之后日志记录等待 `499.3s`。实时日志最后更新时间为 11:25:20，等待期内没有新输出不代表进程退出。
- `127.0.0.1:9333` 仍处于 `Listen`，拥有进程为独立 Edge（PID `24300`）；BOSS 采集错误日志为空。
- 当前数据库：`jd_pool=126266`；`linkedin=123849`、`hn=1796`、`boss=621`，BOSS 全部 `status=cleaned`；BOSS `source_detail` 空值/重复均为 0，`duties` 非空 273；`signal=610`（GitHub 150、Blog 460）；`talent_raw=0`。
- 因此前文“未发现 BOSS 采集进程”“当前任务 `idle` / `ready_to_start`”只适用于 2026-08-21 核验时刻，不再代表当前状态。