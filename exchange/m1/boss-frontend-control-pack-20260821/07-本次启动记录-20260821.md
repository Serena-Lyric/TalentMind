# 07 本次启动记录（2026-08-21）

## 目的

验证现有 BOSS 采集模块从 CLI 经 Edge CDP 到 MySQL 的最小闭环，不启动新的代码改造。

## 执行命令

工作目录：`D:\Application\ClaudeCode\repository\TalentMind\backend`

```powershell
.\.venv\Scripts\python.exe -m app.collect.boss_collect_loop `
  --cdp http://127.0.0.1:9333 `
  --user-data-dir "C:\Users\SERENA~1\AppData\Local\Temp\TalentMind-BOSS-Edge-9222" `
  --once
```

## 结果

```text
开始：2026-08-21 09:50:10
完成：2026-08-21 09:54:37
listed=12
details=8
new=0
skipped=12
```

- 进程正常完成并退出。
- 12 条列表岗位均已存在，故 `new=0`、`skipped=12`。
- 本次没有改变 BOSS 数据总量，数据库仍为 `source=boss=488`。
- 该运行输出没有写入持续循环日志文件；脱敏控制台摘录见 `samples/boss-single-run-20260821-console.log`。

## 运行后核验

- 未发现 BOSS 采集 Python 进程，符合 `--once` 预期。
- `127.0.0.1:9333` 监听并返回 HTTP 200。
- CDP `/json` 核验时返回 BOSS `job_detail` 页面目标。
- 端口/页面可用只说明具备启动条件，不表示当前有采集任务在运行。

## 结论

现有 BOSS 采集管线可启动、可完成一轮、可按 URL 幂等跳过已有岗位；但“启动/停止/暂停/恢复/实时进度”尚未封装成前端可调用的统一服务接口，这正是前端控制界面联调需要后端后续适配的部分。
