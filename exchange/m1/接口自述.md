# M1 接口自述（2026-08-15）

- 本模块**只产文件，无对外接口**（D19）。
- 交付物：`exchange/m1/jd.json`（≥100 条清洗后 JD，JSON 数组、snake_case、UTF-8）、`quality_check.md`（抽样核对记录）。
- 消费方：M2（jd.json → 岗位分析）。
- 运行方式：`cd backend && .\.venv\Scripts\python.exe -m app.collect.import_csv --csv <postings.csv> --limit <N>`；导出脚本见集成层。
