> **2026-09-02 M2 完整回包导入与产品流程修复（本轮最新）**：用户明确授权完整导入 `input/岗位数据-新一代与现有/`，不再执行 D55 的英文结构化目录不入主库限制（见 D56）。MySQL 当前 `job_definition=719`、`job_skill=719`、`job_change_log=243`（其中 3 条为 M2 新岗位变更记录，保留原始 `m2_job_id` 但暂未关联内部岗位 ID）；来源覆盖 BOSS/智联/猎聘/LinkedIn/Hacker News。Neo4j 当前 719 个岗位节点、4671 个技能节点、2462 个行业节点，共 7852 节点/11423 关系。`exchange/m2/` 已同步 719 定义、719 合并技能记录和 243 条变更日志，导入审计在 `data/local/m2-import/`（Git 忽略）。
>
> **本轮功能实现**：M2 校验器/导入器兼容包装回包并支持稳定 `m2_job_id`；采集历史页扫描旧 UTF-8/GBK 日志并保存任务日志尾部，控制台支持 BOSS/智联/猎聘平台选择；简历页删除旧目标岗位选择器和单岗位匹配卡片，自动推荐不少于 3 个岗位且展示分不低于 90（同时返回真实 `raw_score`）；Learning 只能由点击的推荐岗位 ID 驱动；图谱加入岗位/技能/行业节点及详情栏独立滚动。

> **历史归档（2026-09-03）**：2026-08-15 ~ 2026-08-31 的采集核验 / 交接 / 回包整合历史块已移入 `docs/superpowers/历史时间线.md`；本文只保留“当前状态 + 资产清单 + 已知限制 + 工程经验 + 维护规则”。

## 一、资产清单（按模块）

### 0. 后端骨架（复用资产，已实现）
- `backend/app/`：`main.py` / `config.py` / `middleware.py` / `response.py` / `db/`(mysql,neo4j,redis) / `llm/client.py` / `skills/normalizer.py` + `skill_dict_seed.json` / `models/schemas.py`
- `backend/tests/`：**236 全量通过**（含 skill_seed / job_analysis / graph / matching / schema / mvp 集成测试）
- 待清理：`models/schemas.py::EmergingJobOut` 旧类、`main.py` “计划 A~E”注释（阶段 7）

### 1. M1 数据采集（A）—— 已实现，可运行
| 项 | 内容 |
|---|---|
| 正式位置 | `backend/app/collect/`（fetchers/cleaner/dedup/pipeline/repository/schema/import_csv） |
| 数据契约 | `backend/app/contracts/ddl.sql`（8 表；change_type 已扩容 VARCHAR(32)、experience 已扩容 VARCHAR(255)，2026-08-15） |
| 数据库现状 | MySQL（2026-09-02 核查）：jd_pool **2983 条 cleaned**（中文三平台：BOSS 1131 + 智联 1088 + 猎聘 764）；LinkedIn/HN 原始 JD 已外部归档（仓库外 `D:\Application\ClaudeCode\repository\TalentMind-external-archives\english-jd-2026-09-01`，仓库内 Git 忽略备份 `data/local/external-jd-archive/2026-09-01`）；signal 1517、talent_raw 0、resume 0（实时值）；分析层 `job_definition/job_skill/job_change_log=719/719/243` 与 Neo4j 7852/11423 见 M2/M3 节与本文件顶部最新块。历史 08-25 快照（jd_pool 127709 等）仅供溯源，旧交接说明见 `exchange/m1/collection-handover-20260825.md` |
| 大型数据 | `docs/originalfile/archive/postings.csv`（493MB / **12.4 万行**（123,849 行，2026-08-16 抽检实测），Git 忽略）；同数据集辅助表 10 个（companies/jobs/mappings，见 exchange/m1/quality_check.md 抽检记录） |
| 交付物（2026-08-17 二次重导） | `exchange/m1/jd.json`（200 条，quality 降序，含 source_detail 与 cross_source，D38/D42）+ `quality_check.md`（10 条抽样已标注通过）+ `cross_validate_report.md`（D42 多源交叉验证 55 对/74 行）+ `接口自述.md` |
| BOSS 采集资产（2026-08-25 更新） | `backend/app/collect/fetch_boss_jobs.py`、`backend/app/collect/fetchers/boss.py`、`backend/app/collect/fetchers/cdp.py`、`backend/app/collect/boss_collect_loop.py`、`backend/tests/test_boss_fetcher.py`、`backend/tests/test_cdp.py`；BOSS 当前 `source=boss` 967 条，全部 `status=cleaned`，source_detail 空值 0；低速循环仍在运行，父/工作进程 PID `20560/23120`，最新日志已完成第 288 轮；9333 页面连接正常；专项测试历史 7 passed；详见 `exchange/m1/boss-collection-handover-20260820.md` 与 `exchange/m1/collection-handover-20260825.md`。

### 2. M2 岗位分析 —— 已迁入 `backend/app/job_analysis/`（阶段 2 完成）
| 项 | 内容 |
|---|---|
| 位置 | `backend/app/job_analysis/`（config/db/differ/export/llm/main/merge/models/pipeline/rules/stage1~3/translate） |
| 功能 | 7 步管道：SQL 解析 → 规则预筛 → 3 个 LLM 模型 → 合并 → diff → 导出 → 中英翻译；**stage3 已接入 skill_dict 约束（D31）** |
| 输入 | M1 导出的 jd_pool SQL/JSON（原 421MB 已删 D27；gz 备份在 `docs/submit/`） |
| 产出 | `exchange/m2/` 当前为 2026-09-01 M2 完整回包：719 条岗位定义、719 条合并技能明细、243 条能力更新日志；技能未完全进入 285 canonical 的候选保留于证据 JSON，并在导入审计中记录 |
| 测试 | `backend/tests/test_job_analysis/`（当前全量回归通过）+ fixtures 小样例（10 条） |
| 运行 | `cd backend && .\.venv\Scripts\python.exe -m app.job_analysis.main --help`（需 LLM_API_KEY） |

### 3. M3 图谱 —— 已迁入 `backend/app/graph/`（阶段 3 完成）
| 项 | 内容 |
|---|---|
| 位置 | `backend/app/graph/builder.py`（+ `__init__.py`） |
| 功能 | 构建 Job/Skill 节点 + REQUIRES / RELATED_TO 边 → `exchange/m3/graph.json`；**节点 id 对齐 job_name/canonical、技能归一（D31）、Response code=0（D29）** |
| 数据源 | 默认 `exchange/m2/job_definition.json`，mock 回退（`exchange/m3/mock_job_data.json`） |
| 测试 | `backend/tests/test_graph.py`（8 通过）；mock 构建 53 节点 / 81 边 |
| 联调 | 已导入 Neo4j（Job 22 / Skill 75 / REQUIRES 191 / RELATED_TO 31）；name_zh 待 M2 回包后由 A 刷新快照 |

### 4. M4 简历匹配 —— 已迁入 `backend/app/matching/`（阶段 5 完成，文本模式）
| 项 | 内容 |
|---|---|
| 位置 | `backend/app/matching/`（skill_extractor/resume_parser/job_parser/matcher/file_parser/generate_test_data/main/resume_matcher_app/canonical） |
| 契约对齐 | matcher 输出技能 canonical 化（D31）；输出含 matched/unmatched/extra，无 status |
| fixtures | `backend/tests/fixtures/matching/`（生成 JSON + 文本样例 + **5 份合成 DOCX/PDF 简历样本**）；UI 原型 → `docs/prototypes/matching/` |
| 真实简历 | `input/人岗匹配/岗位测试用例/*.pdf/*.docx` **未迁移、未提交（D36）** |
| 测试 | `backend/tests/test_matching.py`（5 通过） |
| 待办 | pathfinder 未实现（P0，见 output/M4 要求清单）；resume 落库 P1 |

### 5. M5 前端 —— 已迁入 `frontend/`（阶段 4 完成）
| 项 | 内容 |
|---|---|
| 位置 | `frontend/`（Vue3.5 + TS + Vite4.5 + Element Plus + ECharts + G6 + Pinia + Axios） |
| 页面 | 已重整为 9 个业务页面：Dashboard/Jobs/Graph/Analytics/Collection/Evolution/Resume/Learning/ResumeDemo；**mock 已全部删除**，图谱默认 Career Nebula 星云视图；Learning 已接真实岗位技能目录：M4 pathfinder 回包前按「中文目录 + 简历内存态」生成确定性缺口/阶段（不 mock、不虚构课程） |
| 接口 | 原 20 个统一接口继续保留；新增 `/api/collection/*` 采集控制接口与 `/api/evolution/*` 能力动态只读接口；`request.ts` 统一解包 code=0，正式前端不读本地 Mock |
| 构建 | `pnpm install`（BOM 已修 + approve-builds）+ `pnpm run build` 通过（chunk >500kB 警告，非阻塞） |
| 待办 | M4 pathfinder 返回后可将 Learning 由确定性目录缺口升级为路径建议；继续优化前端 chunk 体积（非阻塞） |



> **2026-08-31 前端完整重新整合**：用户确认采用方案 A，以两张设计图为视觉基准重新组装正式 `frontend/`，不直接覆盖 A 已修复逻辑。已重建统一壳层、正式导航、Career Nebula 图谱、Analytics、Collection 采集控制台和 Evolution 能力动态页；新增 `backend/app/collect/control.py`、`backend/app/routers/collection.py`、`backend/app/routers/evolution.py` 薄接口。真实验证：前端 `pnpm run build` 通过；后端 `pytest` 226 通过/5 失败（5 个既有 M2 语义差异，未因本次前端整合引入）；`/api/collection/status`、`/api/collection/database-stats`、`/api/evolution/sources/stats` 已实测返回统一响应；浏览器逐页冒烟通过。`input/` 原包仍只作只读对照，未迁移真实简历。

> **2026-08-25 页面采集运行更新**：智联父/工作 PID `21840/9724`，最新日志第 249 轮完成，`listed=12/details=8/new=0/skipped=12`；猎聘恢复启动器 `14840`、父/工作 PID `11064/12220`，首轮 `listed=12/details=8/new=8/skipped=4`，均已进入随机等待。数据库为 `zhaopin=622`、`liepin=477`，均 `cleaned`，两平台 `source_detail` 空值 0；猎聘恢复前一次停止原因为详情页触发人工登录/验证，当前首轮已证明人工登录态可用。

> 2026-08-23 方案实施：新增智联/猎聘页面可见 DOM 采集资产：`backend/app/collect/fetchers/job_sites.py`、`backend/app/collect/fetch_cn_jobs.py`、`backend/app/collect/cn_collect_loop.py` 及 `backend/tests/test_cn_job_fetchers.py`。两平台均复用 `CdpClient`、`RawJD`、统一 pipeline 和低速轮换逻辑；当前未修改 DDL。已完成当前登录页冒烟：智联 20 条卡片、猎聘 5 条卡片，扩大批次详情均成功读取；稳定身份支持 `dom://zhaopin/<hash>`，避免无真实职位链接的卡片丢失。正式扩大采集仍以用户已打开页面为优先，遇到登录/验证码/安全验证即停止。

## 二、已知限制（按轻重，含处理状态）

### 高（整合中已解决/已裁决）
1. **技能对齐 skill_dict（反幻觉）** → **已接入（D31）**：stage3 约束抽取 + M3 归一校验 + M4 输出 canonical。⚠️ 预跑 M2 产出为旧逻辑，需约束管道重跑后才与 seed 完全一致。
2. **统一响应 code（0 vs 200）** → **已统一为 0（D29）**：M1 ok()=0、图谱 Response=0、前端校验=0、接口文档已改。
3. **前端 6 页 vs 三页** → **D26 保留 6 页**。
4. ~~421MB seed SQL~~ → **已删除（D27）**，gz 备份保留。
5. ~~`jd_pool.experience VARCHAR(32)` 截断~~ → **已扩容 VARCHAR(255)（2026-08-15）**：ddl.sql + ALTER + repository 去截断；正式通知随会议。

### 复查新增（2026-08-14）

1. ~~`job_change_log.change_type VARCHAR(16)` 超长~~ → **已扩容 VARCHAR(32)（2026-08-15）**：ddl.sql + ALTER 实测 32；正式通知随会议。
2. **`import_change_logs` 潜伏崩溃（已修复）** → INSERT 引用 DDL 不存在的 `object_type` 列，且 M2 产出的 `job_id` 是 job_name（DDL 语义是 job_definition.id）；change_log 为空时未触发。已改为 job_name→id 解析 + 全量重建 + 集成测试。
3. **`job_skill` 与 `job_definition` 无法关联（中）** → 旧 M2 回包曾将 `job_skill.job_name` 翻译为中文，导致与英文定义无法关联；当前主库已改为稳定 key 关联；D56 M2 完整导入后已按 P5 复核（719/719 对齐）。
4. **`resume` 表恒空（低）** → `POST /api/resume/upload` 不落库，dashboard `totalResumes` 恒 0；是否落库待定。
5. **前端类型检查已接入（已修复）** → `typescript ~5.3.3` + `vue-tsc 1.8.27` 兼容；`build` = `vue-tsc --noEmit && vite build`。

### 中（阶段 6/7 处理）
6. 原 20 个接口已全部实现（jobs CRUD/import/export、dashboard 5、graph、resume）；2026-08-31 新增 collection/evolution 管理 API，2026-09-02 新增 learning API，前端统一走真实 API（vite /api 代理）。Learning 已基于中文岗位技能目录与简历内存态生成确定性缺口和阶段建议。
7. M2 完整回包已导入：`job_definition/job_skill/job_change_log=719/719/243`；英文/中文展示字段、分类、稳定 `m2_job_id` 已接入，3 条岗位级 orphan 日志保留原始引用。
8. M4 文件解析依赖已安装（requirements 含 pdfplumber/python-docx/mammoth），合成样本已备。
9. M4 `extract_skills` 对“中文紧邻英文”提取失效（原型 `\b` 边界缺陷，测试已规避；待优化）。
10. 前端 chunk >500kB（体积优化，非阻塞）；pnpm 首次安装需 `pnpm approve-builds --all` 放行 esbuild 等。
11. `exchange/m2` 已更新为本轮完整数组交接文件，旧预跑文件仅作历史追溯。
12. `input/人岗匹配/` 含真实简历，整目录已被 `.gitignore` 忽略（D36 暂缓）；迁移只 add 明确文件，禁止 `git add -A`。
13. 原交付目录已统一归档至 `input/`（`图谱模块/`、`岗位能力图谱前端系统/`、`人岗匹配/`，整目录 gitignore 保护）；旧 `input/jd-filter-package/` 已于 2026-08-30 删除（`input/人岗匹配/` 简历删除前先列清单与用户确认）。

### 低（可选优化）
14. `models/schemas.py::EmergingJobOut`、`main.py` “计划 A~E”注释待清理（阶段 7）。
15. 图谱 `lib/`、`.idea/`、`job.db`、`output/` 已忽略（D28），随 `input/图谱模块/` 归档（整体忽略）清理。
16. 前端 `public/reference.jpg`、`screenshot.png` 等临时素材保留（无害）。

## 二·五、工程经验（与模块职责无关，A / 本地运维参考）

整合过程中踩过、但与各模块二次开发职责无关的工程问题，归档于此（笔记.md 第六节引用本处）：

1. **FastAPI 路由顺序**：静态路径（如 `/jobs/export`）必须注册在参数路径（如 `/jobs/{job_id}`）之前，否则 int 参数先匹配返回 422。
2. **表单上传**：FastAPI 处理 `FormData` 需要 `python-multipart`（已加入 requirements）。
3. **路由前缀**：`APIRouter(prefix=...)` 与 `include_router(prefix=...)` 只在一处设置，避免产生 `/api/api`。
4. **`import_exchange.py` 语义**：job_definition/job_skill 为**全量重建**（先清空再写，M2 产出是权威岗位集合）；CRUD 手动新增的岗位在重导后会被清除。
5. **进程清理**：Windows 下 Ctrl-C 可能不终止 uvicorn/vite 子进程，用 `Get-NetTCPConnection -LocalPort <port>` + `Stop-Process` 清理（AGENTS 规则）。
6. M2 完整回包接入后图谱使用 `job:<m2_job_id>`、`skill:<canonical>`、`industry:<slug>` 稳定节点键，Neo4j 嵌套 evolution 属性序列化保存。
6. **M2 LLM 配置**：`job_analysis/config.py` 通过 `python-dotenv` 读取 `backend/.env`（`LLM_API_KEY/LLM_BASE_URL`，gitignore 保护）。
7. **Neo4j 数据源切换**：换 graph.json 后先 `MATCH (n) DETACH DELETE n` 再导入，避免新旧图混入。
8. **前端联调**：vite dev 需配置 `/api` 代理（已加）；`vite build` 不做类型检查，建议接入 `vue-tsc --noEmit`（见笔记 M5 待办）。
9. **vue-tsc 版本兼容**：`vue-tsc 1.8.x` 与 TypeScript ≥5.4 不兼容（`Search string not found: /supportedTSExtensions/`）；前端需锁 `typescript ~5.3.3`，`build` 已改为 `vue-tsc --noEmit && vite build`。
10. **change_log 导入**：`import_change_logs` 曾引用 DDL 不存在的 `object_type` 列且 M2 `job_id` 为 job_name、DDL 语义为 id；空数据不触发、非空即崩。已修复（job_name→id 解析 + 全量重建），改导入层必须对照 DDL 逐列核对。

## 三、维护规则（所有 Agent 必须遵守）

1. **工作前**：先读本文件 + `docs/superpowers/决策跟踪.md`（已决策 + 未决项）+ `backend/app/contracts/ddl.sql`，确认资产位置、整合状态与未决事项，再动手。
2. **变更后**：新增 / 移动 / 删除 / 迁移资产、修改契约或目录、产出新交接文件后，**必须立即更新本文件**；涉及决策变更时同步更新 `决策跟踪.md`。
3. **协作中**：回发闭环按 `plans/2026-08-14-module-roundtrip.md` 执行（schema 校验 → diff 门禁 → 单测/集成 → 导入+冒烟）；契约变更按 `2026-08-14-remediation-plan.md`；未决项（P1/P2/P3/P5）未裁决前不阻塞。
4. 本文件与 README / CLAUDE / AGENTS 冲突时，以本文件 + 决策跟踪为准，并同步修正其他文档。
5. **测试数据自动清理（D37）**：测试写入 DB/文件的数据必须按测试夹具特征精确清理（finally/teardown），禁止按 source 宽泛删除；运行集成测试后必须查询 DB 验证无测试残留且生产数据完好。
> 2026-08-20 运行记录：BOSS 低速循环已于 17:20:11（+08:00）按新默认节奏启动，启动器 PID 20544、工作进程 PID 35472；17:23:46 首轮完成，`listed=12/details=8/new=0/skipped=12`。当前 BOSS 474 条，全部 cleaned，source_detail 空值/重复/占位 URL 均为 0。


