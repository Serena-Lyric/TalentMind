# TalentMind 资产与状态清单（工作前必读）

> **2026-08-30 前端全页面审计**：已完成正式前端六页的静态检查、真实 API 检查和浏览器视觉验收；审计与分开发者整改清单已写入 `output/2026-08-30-前端全页面审计总览.md` 及同目录 M2/M3/M4/M5/A 五份文件。当前确认 Graph 可真实显示，Dashboard/Jobs/Resume 存在真实数据与演示数据混用，Learning/ResumeDemo 存在空白页，后续按清单整改。

> **2026-08-27 前端/图谱结合验收**：输入包 `input/岗位能力图谱前端系统/` 与 `input/图谱模块/` 已作为只读对照保留；正式 `frontend/` 接入 GraphPanorama 多视图和星云布局，正式图谱 API 已实测返回 97 节点/222 边并在浏览器显示。M3 builder/import_graph 已支持 `job_name_zh`/`name_zh`，待 M2 新回包后重建图谱。前端构建通过，M3 图谱测试与 MVP 集成测试共 19 条通过；完整 pytest 仍有既有采集集成测试因未按 source 过滤同名生产岗位而失败，测试 finally 后夹具计数 0。

> **2026-08-25 23:21:47（数据库时间；本机采集日志显示为 2026-08-26 07:19:53，系统时钟相差约 8 小时，报告分别标注）**：已完成全量采集管线核验并恢复猎聘循环。通用 6 小时循环父/工作 PID `34120/35004`；BOSS 父/工作 PID `20560/23120`；智联父/工作 PID `21840/9724`；猎聘恢复启动器 `pwsh` PID `14840`、循环父/工作 PID `11064/12220`。猎聘恢复首轮（本机日志 `2026-08-26 07:15:54–07:19:53`）`listed=12/details=8/new=8/skipped=4`，现已进入随机等待；错误日志为空。数据库 `jd_pool=127709`（linkedin 123849、hn 1794、boss 967、liepin 477、zhaopin 622），全部 `status=cleaned`；`source_detail` 空值 0；`cross_source=1` 为 886；`signal=1087`（github 264、blog 823）；`talent_raw=0`、`resume=0`。Edge CDP `127.0.0.1:9333` 仍监听，页面目标按域名核验包含智联、BOSS、猎聘，未发现 `about:blank` 目标；未关闭用户页面。详细接手说明见 `exchange/m1/collection-handover-20260825.md`。

> 2026-08-24 23:48（数据库时间）最新核验：通用 6 小时循环父/工作 PID `34120/35004`，BOSS PID `20560/23120`，智联 PID `21840/9724`；猎聘原循环因详情页触发人工登录/验证于上一轮停止，本次已恢复启动器 PID `21024`、循环 PID `31256`、子进程 PID `9516`，恢复后首轮 `listed=12/details=8/new=10/skipped=2` 并进入随机等待。数据库 `jd_pool=127334`（linkedin 123849、hn 1795、boss 890、liepin 357、zhaopin 443），全部为 `cleaned`；`signal=934`（github 234、blog 700）；`cross_source=887`。当前 9333 页面快照未发现此前的 `about:blank` 目标；本次未关闭、未创建或导航该页面。

> 2026-08-23 20:21 页面循环仍存活：智联 PID `21840`、猎聘 PID `5068`、BOSS PID `20560/23120`。数据库最新核验：`jd_pool=126408`（linkedin 123849、hn 1795、boss 708、liepin 34、zhaopin 22），五类来源均为 `cleaned`；智联/猎聘 `source_detail` 空值均为 0；保留的 `about:blank` 页面仍在。

> 2026-08-23 采集状态最新核验：BOSS 低速循环仍在运行，当前循环 PID `20560`（工作进程 `23120`），Edge CDP `127.0.0.1:9333` 正在监听；第 4 轮北京/Java 已开始。数据库实时核验：`jd_pool=126371`（linkedin 123849、hn 1795、boss 704、liepin 11、zhaopin 12），全部 `status=cleaned`；智联/猎聘新增记录的 `source_detail` 均非空且无重复。日志见 `data/local/logs/boss_collect_loop-codex-20260823-191237.out.log`。

> 2026-08-23 20:14 页面可见 DOM 正式采集已启动：智联循环 PID `21004`，猎聘循环 PID `17792`；两者均使用独立低速轮询、每轮 1 页/最多 8 条详情、轮次间随机等待 420–780 秒。猎聘采用已在登录页面实测成功的搜索页面模板（全国代码 `410`），智联使用北京 `530`；不读取 Cookie/密码、不拦截请求、不调用官方或私有接口、不处理验证码。数据库实时核验：`jd_pool=126396`（linkedin 123849、hn 1795、boss 707、liepin 28、zhaopin 17），两平台新增均为 `cleaned`，`source_detail` 空值 0。`about:blank` 目标 `BA87FDA2CAA0F5D94057B0258BCAFA96` 未关闭。

> 2026-08-23 页面冒烟核验：智联当前页识别 20 条卡片，扩大批次读取 8 条详情并新增 9；猎聘当前页识别 5 条卡片，扩大批次读取 5 条详情并新增 5。两平台均只读取已登录 Edge 中的可见 DOM，不读取 Cookie/密码、不拦截请求、不调用私有接口、不处理验证码或安全验证。

> 日期：2026-08-13（整合阶段 0–5 已完成）；2026-08-14（MVP 复查修复，见下方“复查新增（2026-08-14）”）
> 用途：所有 Agent / 开发者开始任何工作前，先读本文件，确认资产位置、整合状态与已知限制；任何新增 / 移动 / 删除 / 迁移资产后必须及时更新本文件。
> 权威依据：`docs/superpowers/决策跟踪.md`（决策 + 未决）、`docs/superpowers/specs/2026-08-03-team-plan-design.md`、`2026-08-11-repository-organization-design.md`、`docs/superpowers/plans/2026-08-14-remediation-plan.md`、`2026-08-14-module-roundtrip.md`；旧实施计划（08-08 六份 + 08-13 整合计划）已归档 `plans/archive/`
> 修正方案：`docs/superpowers/plans/2026-08-14-remediation-plan.md`（系统修正 v2）、`2026-08-14-module-roundtrip.md`（外发-回发闭环）；`input/` 为队员原版对比参考（已 gitignore，含真实简历禁止入库）
> 2026-08-15 执行：`validate_exchange.py` 交接校验器上线（7 测试通过，change_type 枚举硬校验）；API 中英文过渡生效（title/label 中文 + name_en）；回发包已生成 `output/`（**四包统一 v3**，含快照说明/环境信息/M4 合成简历样本，gitignore，待压缩发送）；M1 数据就绪（jd_pool 5003 cleaned、exchange/m1/jd.json 200 条）；change_type 扩容 VARCHAR(32)、experience 扩容 VARCHAR(255)（正式通知随全队会议）。
> 2026-08-16 执行：定位并修复集成测试误删 jd_pool（根因 `DELETE WHERE source='dataset'` 覆盖生产数据；D37 测试数据精确清理 + finally + 测试后核查）；清库后从 postings.csv 重新导入 **5000 条 cleaned**（原 5003 含 3 条测试残留）；`exchange/m1/jd.json`（200 条）与 `quality_check.md` 重新导出；cleaner experience 提取上限 255（修复单行描述捕获整段致 1406）；`docs/superpowers/archive/2026-08-20/legacy/项目需求.txt` 恢复；确认根目录四个原交付目录已归档 `input/`（gitignore 保护）。
> 2026-08-17 执行：**来源标签 D38**——`jd_pool.source='dataset'`→`'linkedin'`（D17 仅记录平台），新增 `source_detail` 列（posting_domain/数据集标识）；DDL+DB+collect 代码+测试同步，`exchange/m1/jd.json` 按新 source 重导；前端联调数据包 `data/local/frontend-data-pack/` 生成并发送；M2 数据支撑诊断报告（技术岗占比 20%、单源、无时序/信号）；**多源信号采集 P0 启动（D39）**——`signal` 表新增 `source` 列，GitHub Trending（语言 repo_count）+ 技术博客 RSS（技能 mention_count）入库；计划见 `plans/2026-08-17-multisource-collection-plan.md`；**采集模块完整开发（D41）**：`collect/README.md` + 统一入口 `fetch_all.py` + 定时脚本 `scripts/collect_daily.ps1`（计划任务 TalentMindCollect 每日 02:00 已注册）；crawl4ai 评估不引入；**多源交叉验证 P2（D42）**：`jd_pool.cross_source` 列 + `cross_validate.py`，首轮 55 对/74 行，HN 历史扩充后 371 对/426 行，报告 `exchange/m1/cross_validate_report.md`；**持续采集改设计（D44，2026-08-18）**：计划任务自动触发不可靠 → `collect_loop.py` 直接持续采集（每 6h 一轮、后台运行中），`signal` 追加式时间序列（已 2 天多时间点）；**持续采集启动**：HN 抓历史 6 个月（1795 条）、M2 数据包 `data/local/m2-data-pack/` 已生成（jd_pool.sql 全量 6795 条，M2 parse 兼容）、`db.py` 解析器修复（反斜杠转义保留，A 修改文件清单）。
> 2026-08-20 历史记录：BOSS 内部浏览器快照读取 7 个关键词共 105 条列表记录，与 2026-08-19 快照 45 条合并去重后写入 77 条，合计 122 条；这批数据不计作 CDP 正式采集模块首轮结果。此前内部浏览器会话于 2026-08-20 10:48:55（+08:00）退出。
> 2026-08-20 连接核查：`127.0.0.1:9222` 位于 Windows TCP 排除段 `9181-9280`；`127.0.0.1:15721` 是 CC Switch 健康接口而非 CDP；3180 不纳入正式协议。连接层已支持固定端点、`DevToolsActivePort` 动态端口和浏览器级 WebSocket 目标附加。
> 2026-08-20 正式 CDP 首轮：用户在独立 Edge 中人工选择“我要投职”并登录，使用 `http://127.0.0.1:9333` 完成 Python/Java/数据分析 × 北京 × 2 页，`listed=50, details=47, new=29, skipped=21`；入库后 `jd_pool=6947`、`source=boss=151`。
> 2026-08-20 正式 CDP 扩大采集最终状态：8 个关键词 × 北京/上海/深圳 × 每组最多 5 页，`listed=415, details=300, new=319, skipped=96`；原有 BOSS 151 条全部保留，最终扩大批次 `jd_pool=7266`、`source=boss=470`；低速循环第 1–15 轮又新增 14 条，当前 `source=boss=488`；第 16 轮因 CDP 无可用页面安全停止。质量核验：BOSS 全部 `status=cleaned`，`source_detail` 空值/重复组/占位 URL 均为 0；新增 319 条中 215 条 `raw_text` 长度>100、212 条 `duties` 非空。
> BOSS 采集资产：代码见 `backend/app/collect/fetch_boss_jobs.py`、`fetchers/boss.py`、`fetchers/cdp.py`、`boss_collect_loop.py`；专项交接见 `exchange/m1/boss-collection-handover-20260820.md`。当前独立 Edge 仍保持 BOSS 求职端登录，必须由用户人工退出并补录实际注销时间；不得提交登录态。
> Agent 新对话起始：根目录 `AGENT_START_HERE.md`（通用了解路线，必读）；A 角色另读 `A_AGENT_HANDOVER.md`（开发路线 + 交接）
> 整合状态：**数据闭环 + 回发闭环就绪（2026-08-15）**：M2 约束重跑 22 岗位、M3 图谱 97 节点/222 边、20 API、前端 6 页全真实数据；`validate_exchange` 校验器 + 中英文过渡上线；回发包 v3 已生成（output/）待发送；**221 测试通过**。剩余：队员二次开发（8/16–19）、A 验收（8/19–21 错峰）、阶段 7 清理（待用户确认）。

> **2026-08-30 M2/M3/M5 回包整合**：已将 `input/M2交付包/` 的管道源码安全迁入 `backend/app/job_analysis/`（密钥默认值已清除），交付岗位数据复制到 `exchange/m2/` 并导入 MySQL：`job_definition=470`、`job_skill=470`、`job_change_log=0`。基于新 M2 产出重建 M3：`exchange/m3/graph.json=588 节点/4003 边`，导入 Neo4j 后为 `588 节点/3998 边`（MERGE 合并 5 条重复关系）。已清理正式前端 Dashboard 的硬编码统计/趋势/岗位列表，ResumeDemo/Learning 改为诚实空态；旧 `input/jd-filter-package/` 已确认无正式代码引用并删除。

## 一、资产清单（按模块）

### 0. 后端骨架（复用资产，已实现）
- `backend/app/`：`main.py` / `config.py` / `middleware.py` / `response.py` / `db/`(mysql,neo4j,redis) / `llm/client.py` / `skills/normalizer.py` + `skill_dict_seed.json` / `models/schemas.py`
- `backend/tests/`：**221 全量通过**（含 skill_seed / job_analysis / graph / matching / schema / mvp 集成测试）
- 待清理：`models/schemas.py::EmergingJobOut` 旧类、`main.py` “计划 A~E”注释（阶段 7）

### 1. M1 数据采集（A）—— 已实现，可运行
| 项 | 内容 |
|---|---|
| 正式位置 | `backend/app/collect/`（fetchers/cleaner/dedup/pipeline/repository/schema/import_csv） |
| 数据契约 | `backend/app/contracts/ddl.sql`（8 表；change_type 已扩容 VARCHAR(32)、experience 已扩容 VARCHAR(255)，2026-08-15） |
| 数据库现状 | MySQL（2026-08-25 23:21:47）：jd_pool **127709 条 cleaned**（linkedin 123849 + **hn 1794 + boss 967 + liepin 477 + zhaopin 622**）；postings.csv archive 全量 123849 行已导入；**source=linkedin/hn/boss/liepin/zhaopin + source_detail**；talent_raw 0；**signal 1087 条**（github 264 + blog 823）；`jd_pool.cross_source` 数据库实时标记 886 行；数据闭环：skill_dict 285、job_definition 22、job_skill 22、job_change_log 0；完整导出见 `data/local/m2-data-pack/talentmind-db-full-20260823.sql`，M2 说明见 `exchange/m2/m1-database-handover-20260823.md`。详见 `exchange/m1/collection-handover-20260825.md`。
| 大型数据 | `docs/originalfile/archive/postings.csv`（493MB / **12.4 万行**（123,849 行，2026-08-16 抽检实测），Git 忽略）；同数据集辅助表 10 个（companies/jobs/mappings，见 exchange/m1/quality_check.md 抽检记录） |
| 交付物（2026-08-17 二次重导） | `exchange/m1/jd.json`（200 条，quality 降序，含 source_detail 与 cross_source，D38/D42）+ `quality_check.md`（10 条抽样已标注通过）+ `cross_validate_report.md`（D42 多源交叉验证 55 对/74 行）+ `接口自述.md` |
| BOSS 采集资产（2026-08-25 更新） | `backend/app/collect/fetch_boss_jobs.py`、`backend/app/collect/fetchers/boss.py`、`backend/app/collect/fetchers/cdp.py`、`backend/app/collect/boss_collect_loop.py`、`backend/tests/test_boss_fetcher.py`、`backend/tests/test_cdp.py`；BOSS 当前 `source=boss` 967 条，全部 `status=cleaned`，source_detail 空值 0；低速循环仍在运行，父/工作进程 PID `20560/23120`，最新日志已完成第 288 轮；9333 页面连接正常；专项测试历史 7 passed；详见 `exchange/m1/boss-collection-handover-20260820.md` 与 `exchange/m1/collection-handover-20260825.md`。

### 2. M2 岗位分析 —— 已迁入 `backend/app/job_analysis/`（阶段 2 完成）
| 项 | 内容 |
|---|---|
| 位置 | `backend/app/job_analysis/`（config/db/differ/export/llm/main/merge/models/pipeline/rules/stage1~3/translate） |
| 功能 | 7 步管道：SQL 解析 → 规则预筛 → 3 个 LLM 模型 → 合并 → diff → 导出 → 中英翻译；**stage3 已接入 skill_dict 约束（D31）** |
| 输入 | M1 导出的 jd_pool SQL/JSON（原 421MB 已删 D27；gz 备份在 `docs/submit/`） |
| 产出 | `exchange/m2/` 当前为 2026-08-30 回包：实际 470 条岗位定义/470 条技能明细；回包手册写 488 条，数量差异保留在 M2 清单；技能对齐 285 canonical 仍有大量警告 |
| 测试 | `backend/tests/test_job_analysis/`（58 通过）+ fixtures 小样例（10 条） |
| 运行 | `cd backend && .\.venv\Scripts\python.exe -m app.job_analysis.main --help`（需 LLM_API_KEY） |

### 3. M3 图谱 —— 已迁入 `backend/app/graph/`（阶段 3 完成）
| 项 | 内容 |
|---|---|
| 位置 | `backend/app/graph/builder.py`（+ `__init__.py`） |
| 功能 | 构建 Job/Skill 节点 + REQUIRES / RELATED_TO 边 → `exchange/m3/graph.json`；**节点 id 对齐 job_name/canonical、技能归一（D31）、Response code=0（D29）** |
| 数据源 | 默认 `exchange/m2/job_definition.json`，mock 回退（`exchange/m3/mock_job_data.json`） |
| 测试 | `backend/tests/test_graph.py`（8 通过）；mock 构建 53 节点 / 81 边 |
| 联调 | 已导入 Neo4j（Job 22 / Skill 75 / REQUIRES 191 / RELATED_TO 31）；name_zh 待 M2 回包后由 A 刷新快照 |

### 4. M4 简历匹配 —— 已迁入 `backend/app/matching/`（阶段 5 完成，文本模式）
| 项 | 内容 |
|---|---|
| 位置 | `backend/app/matching/`（skill_extractor/resume_parser/job_parser/matcher/file_parser/generate_test_data/main/resume_matcher_app/canonical） |
| 契约对齐 | matcher 输出技能 canonical 化（D31）；输出含 matched/unmatched/extra，无 status |
| fixtures | `backend/tests/fixtures/matching/`（生成 JSON + 文本样例 + **5 份合成 DOCX/PDF 简历样本**）；UI 原型 → `docs/prototypes/matching/` |
| 真实简历 | `input/人岗匹配/岗位测试用例/*.pdf/*.docx` **未迁移、未提交（D36）** |
| 测试 | `backend/tests/test_matching.py`（5 通过） |
| 待办 | pathfinder 未实现（P0，见 output/M4 要求清单）；resume 落库 P1 |

### 5. M5 前端 —— 已迁入 `frontend/`（阶段 4 完成）
| 项 | 内容 |
|---|---|
| 位置 | `frontend/`（Vue3.5 + TS + Vite4.5 + Element Plus + ECharts + G6 + Pinia + Axios） |
| 页面 | 6 页（D26 保留）：Dashboard/Jobs/Graph/Resume/Learning/ResumeDemo；**mock 已全部删除**（2026-08-13），全部走真实 API；Learning/ResumeDemo 为真实空态（待数据源） |
| 接口 | 20 个（`前后端接口对接文档.md`）；`request.ts` 已启用 code=0 校验（D29）；mock 已全部删除（USE_MOCK 已移除） |
| 构建 | `pnpm install`（BOM 已修 + approve-builds）+ `pnpm run build` 通过（chunk >500kB 警告，非阻塞） |
| 待办 | 对接中文 API（title/name_en）P0；隐藏空列/趋势雷达/Learning+ResumeDemo 数据源 P1 |



> **2026-08-25 页面采集运行更新**：智联父/工作 PID `21840/9724`，最新日志第 249 轮完成，`listed=12/details=8/new=0/skipped=12`；猎聘恢复启动器 `14840`、父/工作 PID `11064/12220`，首轮 `listed=12/details=8/new=8/skipped=4`，均已进入随机等待。数据库为 `zhaopin=622`、`liepin=477`，均 `cleaned`，两平台 `source_detail` 空值 0；猎聘恢复前一次停止原因为详情页触发人工登录/验证，当前首轮已证明人工登录态可用。

> 2026-08-23 方案实施：新增智联/猎聘页面可见 DOM 采集资产：`backend/app/collect/fetchers/job_sites.py`、`backend/app/collect/fetch_cn_jobs.py`、`backend/app/collect/cn_collect_loop.py` 及 `backend/tests/test_cn_job_fetchers.py`。两平台均复用 `CdpClient`、`RawJD`、统一 pipeline 和低速轮换逻辑；当前未修改 DDL。已完成当前登录页冒烟：智联 20 条卡片、猎聘 5 条卡片，扩大批次详情均成功读取；稳定身份支持 `dom://zhaopin/<hash>`，避免无真实职位链接的卡片丢失。正式扩大采集仍以用户已打开页面为优先，遇到登录/验证码/安全验证即停止。

## 二、已知限制（按轻重，含处理状态）

### 高（整合中已解决/已裁决）
1. **技能对齐 skill_dict（反幻觉）** → **已接入（D31）**：stage3 约束抽取 + M3 归一校验 + M4 输出 canonical。⚠️ 预跑 M2 产出为旧逻辑，需约束管道重跑后才与 seed 完全一致。
2. **统一响应 code（0 vs 200）** → **已统一为 0（D29）**：M1 ok()=0、图谱 Response=0、前端校验=0、接口文档已改。
3. **前端 6 页 vs 三页** → **D26 保留 6 页**。
4. ~~421MB seed SQL~~ → **已删除（D27）**，gz 备份保留。
5. ~~`jd_pool.experience VARCHAR(32)` 截断~~ → **已扩容 VARCHAR(255)（2026-08-15）**：ddl.sql + ALTER + repository 去截断；正式通知随会议。

### 复查新增（2026-08-14）

1. ~~`job_change_log.change_type VARCHAR(16)` 超长~~ → **已扩容 VARCHAR(32)（2026-08-15）**：ddl.sql + ALTER 实测 32；正式通知随会议。
2. **`import_change_logs` 潜伏崩溃（已修复）** → INSERT 引用 DDL 不存在的 `object_type` 列，且 M2 产出的 `job_id` 是 job_name（DDL 语义是 job_definition.id）；change_log 为空时未触发。已改为 job_name→id 解析 + 全量重建 + 集成测试。
3. **`job_skill` 与 `job_definition` 无法关联（中）** → `exchange/m2/job_skill.json` 的 `job_name` 为中文译名，与 `job_definition.json` 英文名 22/22 零匹配；M2 需补稳定关联字段（决策跟踪 P5）。
4. **`resume` 表恒空（低）** → `POST /api/resume/upload` 不落库，dashboard `totalResumes` 恒 0；是否落库待定。
5. **前端类型检查已接入（已修复）** → `typescript ~5.3.3` + `vue-tsc 1.8.27` 兼容；`build` = `vue-tsc --noEmit && vite build`。

### 中（阶段 6/7 处理）
6. 20 接口已全部实现（jobs CRUD/import/export、dashboard 5、graph、resume）；前端走真实 API（vite /api 代理）。剩余联调随队员回包（8/19–21）。
7. M2 已约束重跑（2026-08-13：424→22 岗位）；当前缺陷为 `job_skill.job_name` 中文译名与 job_definition 英文 key 关联断裂（3/22），待 M2 回包修复（L1–L3）。
8. M4 文件解析依赖已安装（requirements 含 pdfplumber/python-docx/mammoth），合成样本已备。
9. M4 `extract_skills` 对“中文紧邻英文”提取失效（原型 `\b` 边界缺陷，测试已规避；待优化）。
10. 前端 chunk >500kB（体积优化，非阻塞）；pnpm 首次安装需 `pnpm approve-builds --all` 放行 esbuild 等。
11. `exchange/m2` 预跑产出与 `result/` 重复待清理（阶段 7）。
12. `input/人岗匹配/` 含真实简历，整目录已被 `.gitignore` 忽略（D36 暂缓）；迁移只 add 明确文件，禁止 `git add -A`。
13. 原交付目录已统一归档至 `input/`（`图谱模块/`、`岗位能力图谱前端系统/`、`人岗匹配/`，整目录 gitignore 保护）；旧 `input/jd-filter-package/` 已于 2026-08-30 删除（`input/人岗匹配/` 简历删除前先列清单与用户确认）。

### 低（可选优化）
14. `models/schemas.py::EmergingJobOut`、`main.py` “计划 A~E”注释待清理（阶段 7）。
15. 图谱 `lib/`、`.idea/`、`job.db`、`output/` 已忽略（D28），随 `input/图谱模块/` 归档（整体忽略）清理。
16. 前端 `public/reference.jpg`、`screenshot.png` 等临时素材保留（无害）。

## 二·五、工程经验（与模块职责无关，A / 本地运维参考）

整合过程中踩过、但与各模块二次开发职责无关的工程问题，归档于此（笔记.md 第六节引用本处）：

1. **FastAPI 路由顺序**：静态路径（如 `/jobs/export`）必须注册在参数路径（如 `/jobs/{job_id}`）之前，否则 int 参数先匹配返回 422。
2. **表单上传**：FastAPI 处理 `FormData` 需要 `python-multipart`（已加入 requirements）。
3. **路由前缀**：`APIRouter(prefix=...)` 与 `include_router(prefix=...)` 只在一处设置，避免产生 `/api/api`。
4. **`import_exchange.py` 语义**：job_definition/job_skill 为**全量重建**（先清空再写，M2 产出是权威岗位集合）；CRUD 手动新增的岗位在重导后会被清除。
5. **进程清理**：Windows 下 Ctrl-C 可能不终止 uvicorn/vite 子进程，用 `Get-NetTCPConnection -LocalPort <port>` + `Stop-Process` 清理（AGENTS 规则）。
6. **M2 LLM 配置**：`job_analysis/config.py` 通过 `python-dotenv` 读取 `backend/.env`（`LLM_API_KEY/LLM_BASE_URL`，gitignore 保护）。
7. **Neo4j 数据源切换**：换 graph.json 后先 `MATCH (n) DETACH DELETE n` 再导入，避免新旧图混入。
8. **前端联调**：vite dev 需配置 `/api` 代理（已加）；`vite build` 不做类型检查，建议接入 `vue-tsc --noEmit`（见笔记 M5 待办）。
9. **vue-tsc 版本兼容**：`vue-tsc 1.8.x` 与 TypeScript ≥5.4 不兼容（`Search string not found: /supportedTSExtensions/`）；前端需锁 `typescript ~5.3.3`，`build` 已改为 `vue-tsc --noEmit && vite build`。
10. **change_log 导入**：`import_change_logs` 曾引用 DDL 不存在的 `object_type` 列且 M2 `job_id` 为 job_name、DDL 语义为 id；空数据不触发、非空即崩。已修复（job_name→id 解析 + 全量重建），改导入层必须对照 DDL 逐列核对。

## 三、维护规则（所有 Agent 必须遵守）

1. **工作前**：先读本文件 + `docs/superpowers/决策跟踪.md`（已决策 + 未决项）+ `backend/app/contracts/ddl.sql`，确认资产位置、整合状态与未决事项，再动手。
2. **变更后**：新增 / 移动 / 删除 / 迁移资产、修改契约或目录、产出新交接文件后，**必须立即更新本文件**；涉及决策变更时同步更新 `决策跟踪.md`。
3. **协作中**：回发闭环按 `plans/2026-08-14-module-roundtrip.md` 执行（schema 校验 → diff 门禁 → 单测/集成 → 导入+冒烟）；契约变更按 `2026-08-14-remediation-plan.md`；未决项（P1/P2/P3/P5）未裁决前不阻塞。
4. 本文件与 README / CLAUDE / AGENTS 冲突时，以本文件 + 决策跟踪为准，并同步修正其他文档。
5. **测试数据自动清理（D37）**：测试写入 DB/文件的数据必须按测试夹具特征精确清理（finally/teardown），禁止按 source 宽泛删除；运行集成测试后必须查询 DB 验证无测试残留且生产数据完好。
> 2026-08-20 运行记录：BOSS 低速循环已于 17:20:11（+08:00）按新默认节奏启动，启动器 PID 20544、工作进程 PID 35472；17:23:46 首轮完成，`listed=12/details=8/new=0/skipped=12`。当前 BOSS 474 条，全部 cleaned，source_detail 空值/重复/占位 URL 均为 0。
