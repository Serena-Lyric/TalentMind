# TalentMind 项目历史时间线（归档记录）

> **用途**：收纳曾堆叠在 `决策跟踪.md` / `资产与状态.md` / `A_AGENT_HANDOVER.md` 顶部的历史核验与交接记录。**当前状态一律以 `docs/superpowers/资产与状态.md` 与 `docs/superpowers/决策跟踪.md` 为准**，本文件只作溯源，不作为当前依据（D21）。

## 一、资产与状态.md 历史块（2026-08-15 ~ 2026-08-31，含采集/交接/回包日志）

> **2026-08-31 工作交接（历史记录）**：完成必读文档真实性核对与五模块整合核对（报告：`output/2026-08-31-必读文档真实性核对与五模块整合核对报告.md`）。用户裁决：**采集暂停**（数据量已够，暂不采集；循环状态由接手 agent 用 `Get-Process` / `Get-NetTCPConnection -LocalPort 9333` 自行检查，勿臆测 PID）；**M2 补发翻译后岗位数据、M4 补发含 pathfinder 回包**，收到后再完成中文名落库与 Learning 接入；页面集已按 2026-08-31 方案 A 重整为 `frontend/` 业务页面集（Dashboard/Jobs/Graph/Analytics/Collection/Evolution/Resume/Learning/ResumeDemo）；联调端口定案**后端 18000 / 前端 18080**（vite 已默认代理 `/api`→18000，可用 `VITE_PROXY_TARGET` 覆盖）。上述是历史状态；本轮已按 D56 导入 M2 完整 719 岗位，当前主库和图谱以完整回包为准。测试与前端门禁已在 2026-09-02 收口。
>
> **2026-08-31 独立项执行**（用户选中文案方案 A 后完成，不依赖 M2/M4 回包）：① 修复 `MagicStick` 未导入与 `AbilityRadar` 空 dimensions 崩溃（Resume 页跳转阻塞解除，空数据只显示空态）；② 后端 `POST /api/resume/upload` 支持 `target_job_id` 参与匹配；前端上传校验统一（PDF/DOC/DOCX/TXT、≤10MB）并传目标岗位；③ `GET /api/dashboard/trend` 改为按 `signal` 表真实聚合（实测 08-17~08-28 12 天 Top 技能曲线），`skill-radar` 删除全 50 占位改空结构+message；④ Jobs 写操作接真实 API（批量删除/导入/新增/编辑/详情，前端字段映射后端 `JobPayload`）；⑤ ResumeDemo 接 Pinia 最近一次解析结果（`frontend/src/store/resume.ts`）；⑥ 修复采集集成测试未限定 source（取到生产 zhaopin 同名岗位）。验证：后端全量 pytest **232 passed**；前端 `pnpm run build` 通过；D37 查库夹具 0、当前中文 `jd_pool=2983` 未变、`job_definition/job_skill=1973/1973`；vite 18080 代理后端 18000 实测可用。
>
> **2026-08-31 参赛材料初版**（紧急任务，导师要求开会带初版材料）：已按赛题 XH-202621 要求产出 `docs/submit/` 初版材料包——`README.md`（材料清单与缺口）、`作品设计实现方案.md`（v0.1，真实数据+如实标注缺口）、`部署说明.md`、`测试报告.md`（224 通过/5 失败、M2 覆盖率 71.4%、三项准确率待标注）、`02-测试数据/`（100 条 JD 测试集 + Data Architect 新岗位与 Senior PHP Developer 既有岗位图谱 JSON 示例）。待补：三项准确率人工标注报告、M2 翻译数据（canonical/job_name_zh）、M4 pathfinder、图谱截图、PPT、演示视频、报名表。提交截止 2026-09-05。
>
> **2026-08-31 参赛材料/工具补充**：① 100 条 JD 测试集已由 agent 用大模型完成**中文初版标注**（`docs/submit/02-测试数据/100条JD测试集_中文标注版.json` + 标注说明，翻译+is_it+expected_skills，待人工抽检）；② `output/` 已整理为**只放可转发开发者的内容**（5 份开发者清单 + README），日期类文档归档至 `docs/superpowers/archive/2026-08-31/`、整合方案移至 `docs/superpowers/plans/2026-08-31-frontend-integration-plan.md`；③ 新增**快速启动**：根目录 `start_system.bat`（ASCII 引导）+ `scripts/start_system.ps1`（UTF-8 BOM），双击/管理员运行即启动 Docker+后端(18000)+前端(18080)并开浏览器，**不启动采集**；2026-08-31 已实测启动成功（health code=0、前端 200、/api 代理 200）。
>
> **2026-08-31 给 M2 资料包**：已在 `output/给M2资料包/` 组装完整资料包（用户将压缩发给 M2 开发者）：`01-数据/`（**中文平台 JD 2,983 条**：boss 1,131/zhaopin 1,088/liepin 764 + linkedin/hn 200 样例 + skill_dict 285 + signal 1,493 快照）、`02-契约/`（ddl.sql + 数据契约与中英文规则）、`03-测试集/`（100 条原始 + 中文标注版 + 说明）、`04-任务/`（M2 问题清单）、`README.md`（项目情况/数据说明/工作内容/已知问题/期望产出/验收）。
>
> **2026-08-30 工作交接**：当前简历解析后路由异常的排查证据、未提交变更、端口、验证命令和下一步顺序已集中写入 `A_AGENT_HANDOVER.md` 与 `output/2026-08-30-简历解析与学习路径问题交接.md`；接手 Agent 必须先读两份文件，不要把“生成学习路径”误认为已实现。

> **2026-08-30 简历上传 422 修复**：已在 `frontend/src/components/ResumeDiagnosis.vue` 兼容 Element Plus `UploadFile.raw` 与原生 `File`，避免把包装对象作为 multipart 文件发送；后端匹配逻辑未改。M5 清单已记录根因、边界校验和浏览器回归验收要求，陷阱记录见 `docs/superpowers/traps/2026-08-30-resume-upload-uploadfile-wrapper.md`。

> **2026-08-30 前端全页面审计**：已完成正式前端六页的静态检查、真实 API 检查和浏览器视觉验收；审计与分开发者整改清单已写入 `output/2026-08-30-前端全页面审计总览.md` 及同目录 M2/M3/M4/M5/A 五份文件。当前确认 Graph 可真实显示，Dashboard/Jobs/Resume 存在真实数据与演示数据混用，Learning/ResumeDemo 存在空白页，后续按清单整改。

> **2026-08-27 前端/图谱结合验收**：输入包 `input/岗位能力图谱前端系统/` 与 `input/图谱模块/` 已作为只读对照保留；正式 `frontend/` 接入 GraphPanorama 多视图和星云布局，正式图谱 API 已实测返回 97 节点/222 边并在浏览器显示。M3 builder/import_graph 已支持 `job_name_zh`/`name_zh`；已基于新 M2 数据重建（588 节点/4003 边），但 `name_zh` 仍为空（M2 未提供中文，待翻译数据）。前端构建通过，M3 图谱测试与 MVP 集成测试共 19 条通过；完整 pytest 仍有既有采集集成测试因未按 source 过滤同名生产岗位而失败，测试 finally 后夹具计数 0。

> **2026-08-25 23:21:47（数据库时间；本机采集日志显示为 2026-08-26 07:19:53，系统时钟相差约 8 小时，报告分别标注）**：已完成全量采集管线核验并恢复猎聘循环。通用 6 小时循环父/工作 PID `34120/35004`；BOSS 父/工作 PID `20560/23120`；智联父/工作 PID `21840/9724`；猎聘恢复启动器 `pwsh` PID `14840`、循环父/工作 PID `11064/12220`。猎聘恢复首轮（本机日志 `2026-08-26 07:15:54–07:19:53`）`listed=12/details=8/new=8/skipped=4`，现已进入随机等待；错误日志为空。数据库 `jd_pool=127709`（linkedin 123849、hn 1794、boss 967、liepin 477、zhaopin 622），全部 `status=cleaned`；`source_detail` 空值 0；`cross_source=1` 为 886；`signal=1087`（github 264、blog 823）；`talent_raw=0`、`resume=0`。Edge CDP `127.0.0.1:9333` 仍监听，页面目标按域名核验包含智联、BOSS、猎聘，未发现 `about:blank` 目标；未关闭用户页面。详细接手说明见 `exchange/m1/collection-handover-20260825.md`。

> 2026-08-24 23:48（数据库时间）最新核验：通用 6 小时循环父/工作 PID `34120/35004`，BOSS PID `20560/23120`，智联 PID `21840/9724`；猎聘原循环因详情页触发人工登录/验证于上一轮停止，本次已恢复启动器 PID `21024`、循环 PID `31256`、子进程 PID `9516`，恢复后首轮 `listed=12/details=8/new=10/skipped=2` 并进入随机等待。数据库 `jd_pool=127334`（linkedin 123849、hn 1795、boss 890、liepin 357、zhaopin 443），全部为 `cleaned`；`signal=934`（github 234、blog 700）；`cross_source=887`。当前 9333 页面快照未发现此前的 `about:blank` 目标；本次未关闭、未创建或导航该页面。

> 2026-08-23 20:21 页面循环仍存活：智联 PID `21840`、猎聘 PID `5068`、BOSS PID `20560/23120`。数据库最新核验：`jd_pool=126408`（linkedin 123849、hn 1795、boss 708、liepin 34、zhaopin 22），五类来源均为 `cleaned`；智联/猎聘 `source_detail` 空值均为 0；保留的 `about:blank` 页面仍在。

> 2026-08-23 采集状态最新核验：BOSS 低速循环仍在运行，当前循环 PID `20560`（工作进程 `23120`），Edge CDP `127.0.0.1:9333` 正在监听；第 4 轮北京/Java 已开始。数据库实时核验：`jd_pool=126371`（linkedin 123849、hn 1795、boss 704、liepin 11、zhaopin 12），全部 `status=cleaned`；智联/猎聘新增记录的 `source_detail` 均非空且无重复。日志见 `data/local/logs/boss_collect_loop-codex-20260823-191237.out.log`。

> 2026-08-23 20:14 页面可见 DOM 正式采集已启动：智联循环 PID `21004`，猎聘循环 PID `17792`；两者均使用独立低速轮询、每轮 1 页/最多 8 条详情、轮次间随机等待 420–780 秒。猎聘采用已在登录页面实测成功的搜索页面模板（全国代码 `410`），智联使用北京 `530`；不读取 Cookie/密码、不拦截请求、不调用官方或私有接口、不处理验证码。数据库实时核验：`jd_pool=126396`（linkedin 123849、hn 1795、boss 707、liepin 28、zhaopin 17），两平台新增均为 `cleaned`，`source_detail` 空值 0。`about:blank` 目标 `BA87FDA2CAA0F5D94057B0258BCAFA96` 未关闭。

> 2026-08-23 页面冒烟核验：智联当前页识别 20 条卡片，扩大批次读取 8 条详情并新增 9；猎聘当前页识别 5 条卡片，扩大批次读取 5 条详情并新增 5。两平台均只读取已登录 Edge 中的可见 DOM，不读取 Cookie/密码、不拦截请求、不调用私有接口、不处理验证码或安全验证。

> 日期：2026-08-13（整合阶段 0–5 已完成）；2026-08-14（MVP 复查修复，见下方“复查新增（2026-08-14）”）
> 用途：所有 Agent / 开发者开始任何工作前，先读本文件，确认资产位置、整合状态与已知限制；任何新增 / 移动 / 删除 / 迁移资产后必须及时更新本文件。
> 权威依据：`docs/superpowers/决策跟踪.md`（决策 + 未决）、`docs/superpowers/specs/2026-08-03-team-plan-design.md`、`2026-08-11-repository-organization-design.md`、`docs/superpowers/plans/2026-08-14-remediation-plan.md`、`2026-08-14-module-roundtrip.md`；旧实施计划（08-08 六份 + 08-13 整合计划）已归档 `plans/archive/`
> 修正方案：`docs/superpowers/plans/2026-08-14-remediation-plan.md`（系统修正 v2）、`2026-08-14-module-roundtrip.md`（外发-回发闭环）；`input/` 为队员原版对比参考（已 gitignore，含真实简历禁止入库）
> 2026-08-15 执行：`validate_exchange.py` 交接校验器上线（7 测试通过，change_type 枚举硬校验）；API 中英文过渡生效（title/label 中文 + name_en）；回发包已生成 `output/`（**四包统一 v3**，含快照说明/环境信息/M4 合成简历样本，gitignore，待压缩发送）；M1 数据就绪（jd_pool 5003 cleaned、exchange/m1/jd.json 200 条）；change_type 扩容 VARCHAR(32)、experience 扩容 VARCHAR(255)（正式通知随全队会议）。
> 2026-08-16 执行：定位并修复集成测试误删 jd_pool（根因 `DELETE WHERE source='dataset'` 覆盖生产数据；D37 测试数据精确清理 + finally + 测试后核查）；清库后从 postings.csv 重新导入 **5000 条 cleaned**（原 5003 含 3 条测试残留）；`exchange/m1/jd.json`（200 条）与 `quality_check.md` 重新导出；cleaner experience 提取上限 255（修复单行描述捕获整段致 1406）；`docs/superpowers/archive/2026-08-20/legacy/项目需求.txt` 恢复；确认根目录四个原交付目录已归档 `input/`（gitignore 保护）。
> 2026-08-17 执行：**来源标签 D38**——`jd_pool.source='dataset'`→`'linkedin'`（D17 仅记录平台），新增 `source_detail` 列（posting_domain/数据集标识）；DDL+DB+collect 代码+测试同步，`exchange/m1/jd.json` 按新 source 重导；前端联调数据包 `data/local/frontend-data-pack/` 生成并发送；M2 数据支撑诊断报告（技术岗占比 20%、单源、无时序/信号）；**多源信号采集 P0 启动（D39）**——`signal` 表新增 `source` 列，GitHub Trending（语言 repo_count）+ 技术博客 RSS（技能 mention_count）入库；计划见 `plans/2026-08-17-multisource-collection-plan.md`；**采集模块完整开发（D41）**：`collect/README.md` + 统一入口 `fetch_all.py` + 定时脚本 `scripts/collect_daily.ps1`（计划任务 TalentMindCollect 每日 02:00 已注册）；crawl4ai 评估不引入；**多源交叉验证 P2（D42）**：`jd_pool.cross_source` 列 + `cross_validate.py`，首轮 55 对/74 行，HN 历史扩充后 371 对/426 行，报告 `exchange/m1/cross_validate_report.md`；**持续采集改设计（D44，2026-08-18）**：计划任务自动触发不可靠 → `collect_loop.py` 直接持续采集（每 6h 一轮、后台运行中），`signal` 追加式时间序列（已 2 天多时间点）；**持续采集启动**：HN 抓历史 6 个月（1795 条）、M2 数据包 `data/local/m2-data-pack/` 已生成（jd_pool.sql 全量 6795 条，M2 parse 兼容）、`db.py` 解析器修复（反斜杠转义保留，A 修改文件清单）。
> 2026-08-20 历史记录：BOSS 内部浏览器快照读取 7 个关键词共 105 条列表记录，与 2026-08-19 快照 45 条合并去重后写入 77 条，合计 122 条；这批数据不计作 CDP 正式采集模块首轮结果。此前内部浏览器会话于 2026-08-20 10:48:55（+08:00）退出。
> 2026-08-20 连接核查：`127.0.0.1:9222` 位于 Windows TCP 排除段 `9181-9280`；`127.0.0.1:15721` 是 CC Switch 健康接口而非 CDP；3180 不纳入正式协议。连接层已支持固定端点、`DevToolsActivePort` 动态端口和浏览器级 WebSocket 目标附加。
> 2026-08-20 正式 CDP 首轮：用户在独立 Edge 中人工选择“我要投职”并登录，使用 `http://127.0.0.1:9333` 完成 Python/Java/数据分析 × 北京 × 2 页，`listed=50, details=47, new=29, skipped=21`；入库后 `jd_pool=6947`、`source=boss=151`。
> 2026-08-20 正式 CDP 扩大采集最终状态：8 个关键词 × 北京/上海/深圳 × 每组最多 5 页，`listed=415, details=300, new=319, skipped=96`；原有 BOSS 151 条全部保留，最终扩大批次 `jd_pool=7266`、`source=boss=470`；低速循环第 1–15 轮又新增 14 条，当前 `source=boss=488`；第 16 轮因 CDP 无可用页面安全停止。质量核验：BOSS 全部 `status=cleaned`，`source_detail` 空值/重复组/占位 URL 均为 0；新增 319 条中 215 条 `raw_text` 长度>100、212 条 `duties` 非空。
> BOSS 采集资产：代码见 `backend/app/collect/fetch_boss_jobs.py`、`fetchers/boss.py`、`fetchers/cdp.py`、`boss_collect_loop.py`；专项交接见 `exchange/m1/boss-collection-handover-20260820.md`。当前独立 Edge 仍保持 BOSS 求职端登录，必须由用户人工退出并补录实际注销时间；不得提交登录态。
> Agent 新对话起始：根目录 `AGENT_START_HERE.md`（通用了解路线，必读）；A 角色另读 `A_AGENT_HANDOVER.md`（开发路线 + 交接）
> 整合状态：**数据闭环 + 回发闭环就绪（2026-08-15）**：M2 约束重跑 22 岗位、M3 图谱 97 节点/222 边、20 API、前端 6 页全真实数据；`validate_exchange` 校验器 + 中英文过渡上线；回发包 v3 已生成（output/）待发送；**221 测试通过**。剩余：队员二次开发（8/16–19）、A 验收（8/19–21 错峰）、阶段 7 清理（待用户确认）。

> **2026-08-30 M2/M3/M5 回包整合**：已将 `input/M2交付包/` 的管道源码安全迁入 `backend/app/job_analysis/`（密钥默认值已清除），交付岗位数据复制到 `exchange/m2/` 并导入 MySQL：`job_definition=470`、`job_skill=470`、`job_change_log=0`。基于新 M2 产出重建 M3：`exchange/m3/graph.json=588 节点/4003 边`，导入 Neo4j 后为 `588 节点/3998 边`（MERGE 合并 5 条重复关系）。已清理正式前端 Dashboard 的硬编码统计/趋势/岗位列表，ResumeDemo/Learning 改为诚实空态；旧 `input/jd-filter-package/` 已确认无正式代码引用并删除。


## 二、决策跟踪.md「下一步」实施日志（2026-08-03 ~ 2026-09-02）


2026-08-03 已落地：设计文档 specs/2026-08-03-team-plan-design.md、DDL 契约更新、前端说明页 docs/team-plan.html、CLAUDE.md/AGENTS.md 决策要点。剩余未决：测试集归属、里程碑细化、部署演示材料。

2026-08-08 已落地：旧文档全部归档（D21）+ 有效信息汇总 + 设计文档/前端页同步 D19 + 实施计划 6 份（plan0/M1~M5，每份强调 D20 轻量惯例）。

2026-08-11 已落地：确认仓库双重角色（D22）、单一代码源与目录边界（D23）、旧代码清理门禁（D24），并新增 `specs/2026-08-11-repository-organization-design.md`。剩余未决：测试集归属（已建议 M2+M4 构建、A 验收）、里程碑细化、部署演示材料。

2026-08-11 补充：根目录 `人岗匹配/` 的迁移暂缓，保留原状，待用户另行确认后再按设计文档盘点和迁移。

2026-08-13 已落地：整合未决项全部裁决（D26–D36）：目录/范围定稿并解除 D25、421MB seed SQL 删除、统一响应 code=0、接口以 20 个为基线、M2 接入 skill_dict 约束、change_type/experience 契约调整（experience 为后续整理项）、前端 status 语义、zip 删除确认、人岗匹配脱敏入库暂缓。整合实施见 `plans/2026-08-13-system-integration-plan.md`。剩余未决：测试集归属（P1）、里程碑细化（P2）、部署演示材料（P3）。

2026-08-13 数据闭环：M2 以 skill_dict 约束重跑（424 条→22 岗位，$0.94），M3 图谱基于 M2 产出重建（97 节点/222 边）并导入 Neo4j；前端 mock 全部删除、文件解析依赖接入（安装即用）；`import_exchange` 改为全量重建（M2 产出为权威岗位集合）。

2026-08-14/15 复查与回发准备：修复 `import_change_logs` 潜伏崩溃（P4 陷阱）；前端 vue-tsc 接入 build；validate_exchange schema 校验器（change_type 枚举硬校验）；中英文过渡（API title 中文/name_en）；change_type 扩容 VARCHAR(32)、experience 扩容 VARCHAR(255）（均待会议正式通知）；M1 数据就绪（jd_pool 5003 cleaned、exchange/m1/jd.json 200 条）；回发包 v3 已生成（output/）。旧实施计划（08-08 六份 + 08-13 整合计划）已归档 plans/archive/，现行依据为 08-03/08-11 specs + 08-14 两份方案 + 决策跟踪。

2026-08-14 复查修复：修复 `import_change_logs` 潜伏崩溃（object_type 列不存在 + job_id 语义不符，已加测试）；修复前端类型检查失效（TS 锁 5.3.3 + build 接入 vue-tsc）；新增未决 P4（change_type 扩容，通知后执行）、P5（job_skill 关联字段）。详见笔记.md 第九节。

2026-08-16 已落地：修复集成测试破坏性清理（按测试夹具精确删除 + finally；跑完 23 个集成测试后核查：测试残留 0、jd_pool 5000 条生产数据完好）；清库后重新导入 postings.csv 5000 行 cleaned（原 5003 曾被集成测试误删，见 traps/2026-08-16-integration-test-wiped-jd-pool.md）；cleaner experience 提取上限 255（修复单行描述捕获整段致 1406，见 traps/2026-08-16-cleaner-experience-overlength.md）；重新导出 exchange/m1 交付物；恢复 docs/superpowers/archive/2026-08-20/legacy/项目需求.txt；确认根目录四个原交付目录已归档 input/。新增 D37。剩余未决：P1/P2/P3/P5 不变。

2026-08-17 已落地：来源标签 D38（`jd_pool.source='linkedin'` + 新增 `source_detail` 列，DDL/DB/代码/测试/`jd.json` 重导全部完成，198 测试通过）；前端联调数据包已生成于 `data/local/frontend-data-pack/` 并发送前端开发者；M2 数据支撑诊断（技术岗占比低 20%、单源、无时序/信号）已出报告；多源采集（GitHub/RSS P0、中文平台 P1）计划提前待启动；全量导入继续挂起。

2026-08-31 已落地：完成必读文档真实性核对与五模块整合核对，并按用户确认的方案 A 完成前端完整重新整合。采集按用户指示暂停；M2 翻译数据与 M4 pathfinder 仍待回包。正式 `frontend/` 现包含 Dashboard、Jobs、Graph、Analytics、Collection、Evolution、Resume、Learning、ResumeDemo；图谱默认 Career Nebula，采集控制台通过 `backend/app/collect/control.py` 包装既有 M1 循环且不自动启动。新增 `/api/collection/*` 与 `/api/evolution/*` 路由，前端构建与浏览器逐页冒烟通过。后端全量测试 226 通过 / 5 失败，失败均为既有 M2 语义差异；D37 查库核验夹具 0、生产 jd_pool 128625、job_definition 470。

2026-09-02 已落地：完成整体修复阶段第一轮收口；前端构建、后端全量测试、交接校验和数据库清理核验通过。修复 M2 stage3 canonical/alias 约束、翻译层稳定关联 key、低质量分保留语义及对应旧测试契约；英文 JD 外部归档并从主库移出；采集管理页自动准备独立 Edge，已登录时单轮尝试、未登录提示。随后用户明确选择完整导入 M2 719 岗位，D56 覆盖 D55 的结构化英文目录限制。M2 完整导入落地：MySQL `job_definition/job_skill/job_change_log=719/719/243`（3 条岗位级变化日志保留 `m2_job_id` 审计），Neo4j 7852 节点/11423 关系。



## 三、A_AGENT_HANDOVER.md 历史块（数据快照 截至 2026-08-23 / BOSS 低速运行记录 2026-08-20）

- **数据闭环**：jd_pool **126330 cleaned**（linkedin 123849 + hn 1796 + boss 685）；signal **680 条 / 6 个日期**（github 174、blog 506）；cross_source **888 行（数据库实时值；最新报告 887 行，保留 1 条历史残留标记）**；skill_dict 285；job_definition 22；job_skill 22；job_change_log 0；完整数据库导出位于 `data/local/m2-data-pack/talentmind-db-full-20260823.sql`，交接说明见 `exchange/m2/m1-database-handover-20260823.md`。BOSS 当前快照见 `exchange/m1/collection-status-20260823.md`。
- **通用持续采集运行中（D44）**：`collect_loop.py --hours 6 --forever` 父/工作进程 PID `34120/35004` 仍在运行；BOSS 独立循环已于 2026-08-23 09:44:57 +08:00 重启，父/工作进程 PID `22980/36524`，第 1 轮于 09:47:37 完成并记录 `new=2`，随后进入随机等待；9333 与 BOSS 页面连接正常。两条循环均不依赖 SYSTEM 计划任务，后者仅保留为尽力触发。
- **M2 数据包已发**：`data/local/m2-data-pack/` 原有 2026-08-19-1 原始输入包仍保留；本次新增完整数据库导出 `talentmind-db-full-20260823.sql`（534,151,037 bytes，SHA-256 见 `exchange/m2/m1-database-handover-20260823.md`），需通过安全渠道单独发送，不能随 GitHub 提交。
- **测试**：后端 221 全量通过，包含 BOSS CDP 连接与详情归一化测试；前端 `pnpm run build` 通过。
- **服务**：后端 uvicorn :8000、前端 vite :5173；MySQL/Neo4j/Redis 在 Docker。
- **回发闭环**：`output/` 四包 v3 已发队员，二次开发窗口 8/16–19，A 验收 8/19–21（错峰：M2 8/19 → M3/M4 8/20 → M5 8/21）。
- **契约/决策**：D37 测试数据安全清理、D38 source=linkedin+source_detail、D39 多源信号、D40 HN 岗位、D41 采集模块完整开发、D42 交叉验证、D43 M2 数据包、D44 持续采集改设计、D45–D48 BOSS 受控 CDP 采集与 9333 端口、D49 BOSS 扩大采集结果；未决 P1/P2/P3/P6（P5 当前中文桥接目录已完成，M2 新回包到达后复核；P6=其他中文平台合规方案，BOSS 已进入可运行受控方案）。
- **A 集成修改文件清单（回包防覆盖）**：`job_analysis/db.py`（解析器反斜杠转义保留，M2 数据包依赖）；`collect/` 系列（fetch_all/collect_loop/cross_validate/fetch_signals/fetch_hn_jobs/hn_hiring/trending/blog_rss、cleaner/repository/schema/dataset）；`ddl.sql`（experience/change_type 扩容、source_detail、cross_source、signal.source）。

## 2026-08-20 低速持续采集

- 已新增 `backend/app/collect/boss_collect_loop.py`，BOSS 独立于通用 `collect_loop.py` 运行。
- 默认单关键词/城市、1 页、最多 12 条岗位/8 条详情；页面间隔 15–30 秒，关键词/城市切换间隔 6–12 分钟，默认不间断运行。
- 检测到登录页或验证页立即停止；不实现验证码/登录/反爬绕过。日志为 `data/local/logs/boss_collect_loop.out.log`。
- 当前运行记录（2026-08-20 17:20:11 +08:00）：BOSS 低速循环已按新默认节奏重启，启动器 PID 20544、工作进程 PID 35472；首轮北京/Python 于 17:23:46 完成，`listed=12/details=8/new=0/skipped=12`，随后等待 575.8 秒切换。

