# 智联/猎聘 DOM 回归测试自身引号错误

- 症状：新增 DOM 选择器回归测试在收集阶段出现 `SyntaxError: unterminated string literal`。
- 根因：测试断言字符串把单引号和双引号拼接成了未闭合的 Python 字符串。
- 修复：改为合法且直接的字符串断言，并重新执行聚焦测试。
- 教训：测试代码也必须先通过语法收集；涉及引号的断言应保持最小、可读，避免多层转义。
