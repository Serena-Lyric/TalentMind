# TalentMind 决策跟踪

> 维护规则：每次讨论产生新决策或未决项，先更新本文件；全部决策完成后，统一整理成要点写入 CLAUDE.md 与 AGENTS.md（收尾任务，见 D10）。
> 最后更新：2026-08-02

## 一、已决策

| # | 决策 | 说明 |
|----|------|------|
| D1 | 五人分工 | 1 数据采集（爬取简历/岗位原始数据、构建原始库）；2 岗位分析（原始数据→结构化岗位信息，含新岗位发现/动态演化）；3 图谱节点/关系解析（岗位关系→点和线）；4 简历解析+岗位匹配；5 前端（所有岗位/图谱/岗位匹配三个页面） |
| D2 | 协作模式 | 5 人 5 台电脑；主数据库在 A 电脑；最终在 A 电脑合并所有模块；其他电脑默认只放自己模块的文件 |
| D3 | 模块交接模式 | **文件交接 + A 唯一集成**（已确认）：各模块本地独立开发，产出标准格式数据文件/可运行模块，由 A 导入主库、构建统一 API、合并部署 |
| D4 | API 归属 | API 维护由 A（同学 1）完成 |
| D5 | 技术栈 | 各模块自行决定、效果优先，只约定数据格式与对接接口，不限制框架 |
| D6 | 规划顺序 | 需求 → 功能模块 → 产出-消费矩阵 → 数据契约 → API；旧 v2 的 API 清单作废，不作为约束 |
| D7 | 整体方案 | 重置规划骨架（以需求为起点），保留已实现资产（采集代码、docker-compose、DDL 基础、测试） |
| D8 | 岗位定义字段 | 岗位定义需包含：岗位名称、核心职责、必备技能、加分技能、典型行业应用场景；另加：收集时间、来源 |
| D9 | 集成形态 | 契约式单体：最终系统单点可部署（A 机器），对外接口统一契约 |
| D10 | 收尾任务 | 全部决策完成后，将本次对话所有决策整理成要点，更新 CLAUDE.md 与 AGENTS.md |

## 二、未决 / 待修改事项

| # | 事项 | 状态/方向 | 影响 |
|----|------|-----------|------|
| P1 | 数据库定位：现有表仅覆盖爬取原始数据（jd_pool/talent_raw），**角色 2 岗位分析产出数据的表结构未规定** | 待设计：按 D8 字段结构化（改造 emerging_job/job_skill 或新建表） | DDL 修改需全队确认后再动 |
| P2 | 图谱节点/关系数据的存放方式未定：数据库（Neo4j？）／文件／前端临时存储／其他 | 待按"岗位定义五要素"流程完善后决定 | 决定角色 3 产出形态与角色 5 取数方式 |
| P3 | 各模块产出文件的标准格式：JSON/CSV 字段定义、命名、目录约定 | 待定（产出-消费矩阵阶段给出） | 决定"文件交接"能否落地 |
| P4 | 前端说明页形态 | 默认静态单 HTML（docs/team-plan.html），待用户确认 | 最终交付物 |
| P5 | ≥100 条 JD 标注测试集与三项准确率 ≥90% 的验证方案归属 | 待定 | 实用价值 30 分 |
| P6 | 里程碑节奏 | 粗排 4 周，待定稿 | 排期 |
| P7 | 仓库整理 | 已忽略超限大文件（archive.zip 158.8MB、postings.csv 492.9MB）；archive 其余小 CSV 已入库作数据样本 | 仓库整洁 |
| P8 | 部署与演示 | docker-compose 部署说明、演示材料待补 | 提交要求"可部署运行" |

## 三、下一步

产出"需求 → 模块 → 产出-消费矩阵 → 数据契约"规划草稿，逐项关闭 P1–P6。
