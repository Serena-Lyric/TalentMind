# TalentMind 决策跟踪

> 维护规则：每次讨论产生新决策或未决项，先更新本文件；全部决策完成后，统一整理成要点写入 CLAUDE.md 与 AGENTS.md（收尾任务，见 D10）。
> 最后更新：2026-08-17

## 一、已决策

| # | 决策 | 说明 |
|----|------|------|
| D1 | 五人分工 | 1 数据采集（爬取简历/岗位原始数据、构建原始库）；2 岗位分析（原始数据→结构化岗位信息，含新岗位发现/动态演化）；3 图谱节点/关系解析（岗位关系→点和线）；4 简历解析+岗位匹配；5 前端（所有岗位/图谱/岗位匹配三个页面） |
| D2 | 协作模式 | 5 人 5 台电脑；主数据库在 A 电脑；最终在 A 电脑合并所有模块；其他电脑默认只放自己模块的文件 |
| D3 | 模块交接模式 | **文件交接 + A 唯一集成**（已确认）：各模块本地独立开发，产出标准格式数据文件/可运行模块，由 A 导入主库、构建统一 API、合并部署 |
| D4 | API 归属 | API 维护由 A（同学 1）完成 |
| D5 | 技术栈 | 各模块自行决定、效果优先，只约定数据格式与对接接口，不限制框架；旧计划中的"计划级选型与全局约束"仅作**优先考虑**（参考，非约束） |
| D6 | 规划顺序 | 需求 → 功能模块 → 产出-消费矩阵 → 数据契约 → API；旧 v2 的 API 清单作废，不作为约束 |
| D7 | 整体方案 | 重置规划骨架（以需求为起点），保留已实现资产（采集代码、docker-compose、DDL 基础、测试） |
| D8 | 岗位定义字段 | 岗位名称、核心职责、必备技能、加分技能、典型行业应用场景 + 收集时间、来源；source 仅记录来源平台，交叉验证质量分用独立字段 quality（见 D17） |
| D9 | 集成形态 | 契约式单体：最终系统单点可部署（A 机器），对外接口统一契约 |
| D10 | 收尾任务 | 全部决策完成后，将本次对话所有决策整理成要点，更新 CLAUDE.md 与 AGENTS.md（含交接文件格式等关键信息） |
| D11 | 岗位分析数据模型（P1 定稿） | 新增 `job_definition`（岗位定义主表，五要素字段化 + source/quality/collected_at/is_emerging/evolution/updated_at）；改造 `job_skill`（证据链明细：weight/confidence/evidence/is_required）；新增 `job_change_log`（能力变更审计：新增/删除/修改 + 数据源 + 说明） |
| D12 | is_emerging 判定 | 由 M2 实现：聚类 + 与既有岗位比对 + LLM 候选 + 人工确认；P0 可先做"词典比对 + LLM 候选"简化版，难度中等 |
| D13 | evolution 演化阶段 | 由 M2 实现：P0 静态阶段分类（萌芽/增长/成熟/衰退，基于技能组合特征，不编造数字）；P1 用多轮采集时间序列算真实趋势 |
| D14 | status 删除 | 岗位定义不含 status/reviewed 审核状态；人工优化以直接编辑 + updated_at 体现（团队后续如需要审核标记再考虑加回） |
| D15 | 图谱存放（P2 定稿） | Neo4j（A 机 docker 命名卷持久化，重启不丢）；M3 只产出 graph.json（nodes/edges）由 A 导入；文件留仓库作双保险 |
| D16 | 交接文件标准（P3 定稿） | UTF-8 JSON 数组、snake_case、exchange/<模块>/ 目录、字段冻结于 backend/app/contracts/；M2/M3 交文件、M4 交可运行模块、M5 对接 API + Mock |
| D17 | source 语义（修正） | source 仅记录来源平台（字符串或平台数组）；交叉验证质量分用独立字段 quality，不混入 source |
| D18 | 前端说明页形态（P4 关闭） | 静态单 HTML：docs/team-plan.html，零依赖、双击即看 |
| D19 | 接口策略 | 固定产出（数据契约先冻结）；接口由各模块自行定义；A 负责集成时统一调整；先联调 MVP，再基于 MVP 二次开发 |
| D20 | 轻量惯例 | 统一响应 {code,message,data}、snake_case、Mock 先行为模块间约定，须在实施计划与各模块计划中重点说明、分别强调 |
| D21 | 旧文档归档 | docs/superpowers 各目录下 archive/ 存放历史版本，仅供参考溯源、不作为当前依据；归档前逐份审查，提取有效信息编写汇总文档 |
| D22 | 仓库双重角色 | `TalentMind` 同时是 A 负责的 M1 数据采集开发仓，也是 M1–M5 全部模块最终集成后的完整系统唯一主仓；其他成员交付物最终迁入本仓库正式位置 |
| D23 | 单一代码源与目录边界 | 后端正式源码只放 `backend/app/`，前端正式源码只放 `frontend/`；`exchange/` 只存交接文件、接口自述和小型 Mock；大型本地数据放 Git 忽略的 `data/local/`；Git 历史保存旧代码，不在工作树长期保留重复旧代码目录 |
| D24 | 旧代码清理门禁 | 旧 `backend` 不整体删除；现有 M1 采集代码继续复用。重复源码只有在迁移、测试和集成验证通过后才清理，无法确认用途的文件先保留并记录理由 |
| D25 | M4 原型迁移暂缓 | 根目录 `人岗匹配/` 暂不迁入 `backend/app/matching/`、`backend/tests/` 或 `frontend/`；保留原状，待用户另行确认后再盘点和迁移 |
| D26 | 整合目录与范围定稿（2026-08-13） | 目录以 08-11 整理设计为准：M2→`backend/app/job_analysis/`、M3→`backend/app/graph/`、M5→`frontend/`、M4→`backend/app/matching/`；`exchange/` 统一小写 `m1~m5`；Mock 放各模块交接目录；**D25 解除**（`人岗匹配/` 可迁移清理）；前端保留 6 页并更新 planM5 范围 |
| D27 | 421MB seed_jd_pool.sql 删除 | `jd-filter-package/data/seed_jd_pool.sql` 删除、不入库、不保留工作树（本地已备份；`docs/submit/seed_jd_pool.sql.gz` 119MB 压缩备份仍在）；M2 输入改为 M1 导出的 jd_pool 数据 |
| D28 | 图谱模块附属物处理 | `exchange/m3/` 交接产物（graph.json / mock）保留；`.idea/`、`lib/`（vis.js）、`job.db`（GBK 样例）、`output/` 整合时忽略或删除 |
| D29 | 统一响应 code 定稿 | 统一 `code=0` 成功（D20）；前端 `request.ts` 校验与接口文档、图谱 `Response` 从 200 改为 0 |
| D30 | 接口基线 | 以前端交付的 **20 个接口**为 A 定稿基线（扩展 08-03 设计 §9 的 MVP 6 接口），整合时更新设计文档 |
| D31 | skill_dict 约束抽取 | M2 stage3 候选限定 `skill_dict`（反幻觉 §10）；用 `人岗匹配/` 的 200+ 同义词表构建 skill_dict 种子（≥200 canonical） |
| D32 | change_type 扩展值 | `duties_changed` / `scenarios_added` / `scenarios_removed` / `evolution_changed` 纳入 DDL 契约注释/枚举，需公告全队 |
| D33 | experience 字段扩容 | `jd_pool.experience` **已扩容 VARCHAR(255)（2026-08-15）**：ddl.sql + MySQL ALTER + repository 去 `[:32]` 截断；正式通知随全队会议 |
| D34 | 前端 status 语义 | 前端 `Job.status` 为**招聘状态**（展示层），岗位定义仍无审核 status（D14 不变） |
| D35 | 揭榜挂帅材料 zip 删除确认 | `docs/originalfile/第一批/第二批揭榜挂帅材料汇总.zip` 确认删除，不恢复 |
| D36 | 人岗匹配脱敏入库暂缓 | `input/人岗匹配/`（原根目录 `人岗匹配/` 已归档 input/）的脱敏入库与简历忽略规则**暂不处理**，待系统整体效果后再决策；迁移时只 add 明确文件，禁止 `git add -A` |
| D37 | 测试数据自动清理与验证（2026-08-16） | 测试写入 DB/文件的数据必须按测试夹具特征精确清理（finally/teardown），禁止按 source 等宽泛条件 DELETE；运行集成测试后 agent 必须查询 DB 验证无测试残留、生产数据未被误删。根因：集成测试 `DELETE WHERE source='dataset'` 曾误删 jd_pool 5000 条（见 traps/2026-08-16-integration-test-wiped-jd-pool.md） |
| D38 | M1 来源标签落地（2026-08-17） | `jd_pool.source` 由 `'dataset'` 改为 `'linkedin'`（来源平台，D17 仅记录平台，不伪造多源）；新增 `jd_pool.source_detail`（posting_domain / 数据集标识 `linkedin_job_postings`）；`exchange/m1/jd.json` 按新 source 重新导出（200 条）；M2 下次重跑后 `job_definition.source` 将变为 `["linkedin"]`。全量导入与多源采集仍挂起/计划中 |

## 二、未决 / 待修改事项

| # | 事项 | 状态/方向 | 影响 |
|----|------|-----------|------|
| P1 | ≥100 条 JD 标注测试集与三项准确率 ≥90% 验证方案归属 | 建议 M2+M4 构建、A 验收，待定稿 | 实用价值 30 分 |
| P2 | 里程碑节奏 | 4 周粗排已定，待各模块细化 | 排期 |
| P3 | 部署说明与演示材料 | docker-compose 部署说明、演示视频待补 | 提交要求"可部署运行" |
| P4 | job_change_log.change_type 列宽 | DDL `VARCHAR(16)` 装不下 D32 扩展枚举（scenarios_removed/evolution_changed 17 字符，MySQL 实测 1406）；**2026-08-15 已扩容 VARCHAR(32)**（ddl.sql 已改），正式通知随全队会议 | 已执行（改字段，通知待会议） |
| P5 | job_skill 与 job_definition 关联 | job_skill.json 的 job_name（中文译名）与 job_definition.json（英文）**19/22 不匹配**（3/22 精确匹配，2026-08-16 复核），证据链无法完整回溯 | M2 输出稳定关联字段（job_name_en / job_id）后 A 重导 |

## 三、下一步

2026-08-03 已落地：设计文档 specs/2026-08-03-team-plan-design.md、DDL 契约更新、前端说明页 docs/team-plan.html、CLAUDE.md/AGENTS.md 决策要点。剩余未决：测试集归属、里程碑细化、部署演示材料。

2026-08-08 已落地：旧文档全部归档（D21）+ 有效信息汇总 + 设计文档/前端页同步 D19 + 实施计划 6 份（plan0/M1~M5，每份强调 D20 轻量惯例）。

2026-08-11 已落地：确认仓库双重角色（D22）、单一代码源与目录边界（D23）、旧代码清理门禁（D24），并新增 `specs/2026-08-11-repository-organization-design.md`。剩余未决：测试集归属（已建议 M2+M4 构建、A 验收）、里程碑细化、部署演示材料。

2026-08-11 补充：根目录 `人岗匹配/` 的迁移暂缓，保留原状，待用户另行确认后再按设计文档盘点和迁移。

2026-08-13 已落地：整合未决项全部裁决（D26–D36）：目录/范围定稿并解除 D25、421MB seed SQL 删除、统一响应 code=0、接口以 20 个为基线、M2 接入 skill_dict 约束、change_type/experience 契约调整（experience 为后续整理项）、前端 status 语义、zip 删除确认、人岗匹配脱敏入库暂缓。整合实施见 `plans/2026-08-13-system-integration-plan.md`。剩余未决：测试集归属（P1）、里程碑细化（P2）、部署演示材料（P3）。

2026-08-13 数据闭环：M2 以 skill_dict 约束重跑（424 条→22 岗位，$0.94），M3 图谱基于 M2 产出重建（97 节点/222 边）并导入 Neo4j；前端 mock 全部删除、文件解析依赖接入（安装即用）；`import_exchange` 改为全量重建（M2 产出为权威岗位集合）。

2026-08-14/15 复查与回发准备：修复 `import_change_logs` 潜伏崩溃（P4 陷阱）；前端 vue-tsc 接入 build；validate_exchange schema 校验器（change_type 枚举硬校验）；中英文过渡（API title 中文/name_en）；change_type 扩容 VARCHAR(32)、experience 扩容 VARCHAR(255）（均待会议正式通知）；M1 数据就绪（jd_pool 5003 cleaned、exchange/m1/jd.json 200 条）；回发包 v3 已生成（output/）。旧实施计划（08-08 六份 + 08-13 整合计划）已归档 plans/archive/，现行依据为 08-03/08-11 specs + 08-14 两份方案 + 决策跟踪。

2026-08-14 复查修复：修复 `import_change_logs` 潜伏崩溃（object_type 列不存在 + job_id 语义不符，已加测试）；修复前端类型检查失效（TS 锁 5.3.3 + build 接入 vue-tsc）；新增未决 P4（change_type 扩容，通知后执行）、P5（job_skill 关联字段）。详见笔记.md 第九节。

2026-08-16 已落地：修复集成测试破坏性清理（按测试夹具精确删除 + finally；跑完 23 个集成测试后核查：测试残留 0、jd_pool 5000 条生产数据完好）；清库后重新导入 postings.csv 5000 行 cleaned（原 5003 曾被集成测试误删，见 traps/2026-08-16-integration-test-wiped-jd-pool.md）；cleaner experience 提取上限 255（修复单行描述捕获整段致 1406，见 traps/2026-08-16-cleaner-experience-overlength.md）；重新导出 exchange/m1 交付物；恢复 docs/superpowers/项目需求.txt；确认根目录四个原交付目录已归档 input/。新增 D37。剩余未决：P1/P2/P3/P5 不变。

2026-08-17 已落地：来源标签 D38（`jd_pool.source='linkedin'` + 新增 `source_detail` 列，DDL/DB/代码/测试/`jd.json` 重导全部完成，198 测试通过）；前端联调数据包已生成于 `data/local/frontend-data-pack/` 并发送前端开发者；M2 数据支撑诊断（技术岗占比低 20%、单源、无时序/信号）已出报告；多源采集（GitHub/RSS P0、中文平台 P1）计划提前待启动；全量导入继续挂起。
