# 2026-09-05 提交基线对齐与部署交接

> 交接人：本轮 Agent（收尾）
> 范围：演示数据口径对齐（717/717/275）、三份 Word 与提交 README 新基线、导入器扩展、部署/数据导入说明、SQL 快照随提交提供。

## 一、当前状态（2026-09-05 实测，权威）

| 数据 | 数值 |
|---|---|
| job_definition / job_skill | 717 / 717（新一代 342 + 现有 375；已删除 AI 测试开发工程师、博士后 2 条） |
| job_change_log | 275（added 187 / scenarios_added 75 / evolution_changed 5 / duties_changed 8；含 32 条演示日志；job_id NULL=3） |
| jd_pool | 2984（BOSS 1133 / 智联 1088 / 猎聘 763） |
| signal / skill_dict / resume | 1540 / 285 / 0 |
| Neo4j / exchange/m3/graph.json | 7845 节点 / 11412 关系（Job 717 / Skill 4671 / Industry 2457；REQUIRES 8866 / APPLIES_TO 2539 / RELATED_TO 7） |
| 后端测试 | 246 passed（Python 3.14.5） |
| 覆盖率（M2 管道语句） | 71.4% |

## 二、本轮做了什么

1. 数据（仓库 + 桌面提交包同步）：
   - 用 `data/local/talentmind-db-full-20260904.sql`（09-04 16:22 全库快照，即文档所描述基线）重置 MySQL：717/717/275、jd_pool 2984、signal 1540。
   - `exchange/m2`：job_definition / job_skill / job_definition_zh 719→717（删除 2 条），job_change_log 243→275（追加 32 条演示，字段取自库中真实行，job_id 用 m2_job_id 以保持可回链）。
   - `exchange/m3/graph.json` + Neo4j：由 `app.integration.rebuild_m2_graph` 从库重导为 7845/11412（注意：`app/graph/builder.py` 是旧简易版，不是真实图谱生成源，勿用）。
   - `exchange/m1/jd.json`：从当前库重新抽样 200 条中文三平台样例（仓库与提交包一致）。
2. 文档：
   - 三份 Word（产品设计 A / 技术设计 B / 测试方案与报告 C）：B、C 数字改新基线；C 增加 Python 3.14.5、修复"1099"错字（猎聘 763）；A 为定性文档未改。
   - 提交包 README、exchange README、exchange/m2 README 数字同步。
   - 开发仓库 6 份状态文档顶部加 2026-09-05 复核校正块（README/AGENTS/CLAUDE/AGENT_START_HERE/A_AGENT_HANDOVER/资产与状态），决策跟踪、未决问题清单更新。
3. 导入器扩展（`backend/app/integration/m2_package.py`）：`load_return_package` 支持两种布局——legacy（`input/岗位数据-新一代与现有/`）与 standard（`exchange/m2/` 的 job_definition.json / job_skill.json / job_change_log.json）。默认行为不变，原有测试不受影响；新增回归测试 `test_standard_exchange_layout_consistent`。
4. 部署/数据导入：
   - 新增 `docs/部署与导入说明.md`（提交包根目录同名同步）。
   - 提交包新增 `部署数据/talentmind-db-full-2026-09-05.sql`（完整 MySQL 快照，8.4MB；与 data/local 快照同内容）。

## 三、关键坑与提醒（务必阅读）

1. **不要直接跑 `import_all()`**：默认读 `input/岗位数据-新一代与现有/`（719/243 原回包），会把演示库覆盖成 719/243（32 条演示日志被冲掉）。今天已踩过并靠重灌快照恢复。
2. **跑完集成测试也要重灌快照（仅开发仓库）**：开发仓库内部分集成测试调用默认 `import_all`（读 input 原回包），会把演示库重置为 719/243；恢复命令见部署说明"方式 A 第 2 步"。 提交物（评委）环境没有 input/，相关用例已加"目录缺失自动 skip"，不会报错。
3. 提交物不含 `input/`（gitignore），因此"从 exchange 复现"必须用**显式路径** `import_all(r'..\exchange\m2')`（本轮回已支持 standard 布局）。
4. 重新生成图谱用 `python -m app.integration.rebuild_m2_graph`（库→graph.json→Neo4j），勿用 `app/graph/builder.py`（旧版输出 867 节点/16009 边，非真实图谱）。
5. 中文目录名运行 docker compose 报 `project name must not be empty`；提交目录需放英文路径或设 `COMPOSE_PROJECT_NAME=talentmind`。

## 四、部署两方式（摘要，详见 docs/部署与导入说明.md）

- 方式 A（推荐）：docker compose up -d → 灌 `部署数据/talentmind-db-full-2026-09-05.sql` → `python -m app.integration.import_graph`（7845/11412）→ 启动前后端。
- 方式 B（无快照）：`import_all(r'..\exchange\m2')`（重建 717/717/275 分析层）+ `python -m app.integration.import_graph`（原始层 jd_pool/signal 需另用快照）。

## 五、测试关系

- 提交物含 backend/tests（246）；`python -m pytest -q` 全量可跑。
- 测试夹具使用 input 原回包（719/243），与演示口径（717/275）不同是预期；跑完测试后按第三节恢复。
- 单测（不写库）：`python -m pytest -m "not integration" -q`。

## 六、待办 / 未决

- 时间演化测试集 450 条（150×3）在提交包内仍无对应数据文件（只有 100-jd-test-cases.json）——文档主素材缺口，需补齐或从文档中降级说明。
- 演示视频、PPT 等参赛材料（P3 剩余）。
- 本轮回未建任何 git commit；如需入库请先 `git status` 审查后再 add（禁止 `git add -A`）。
- GitHub 同步（如需要）在 commit 后执行；`input/`、`output/`、`docs/submit` 等保持本地不同步。