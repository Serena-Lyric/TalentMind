# TalentMind 必读文档真实性核对 + 五模块整合核对报告

- 日期：2026-08-31
- 角色：A（唯一集成者）
- 方法：基础设施/Docker/端口/进程实测；MySQL 与 Neo4j 直连计数；`input/` 四份回包与 `backend/app/`、`frontend/` 逐文件对比；后端全量 pytest（223/6）；启动后端 18000 实测 20 接口；启动前端 dev 18080 验证代理；M3 builder 复跑比对
- 结论先行：**文档主体可信（测试数、库内计数、图计数、模块迁入位置均真实），但有 7 处状态性偏差需修正；五模块"数据链路"真实打通，功能缺口集中在中文名、canonical、pathfinder、写操作与采集进程。**

---

## 一、文档偏差清单（文档声称 vs 实际，需修正）

| # | 文档位置 | 文档声称 | 实际（2026-08-31 实测） | 修正建议 |
|---|---|---|---|---|
| W1 | 资产与状态 08-25 / A_AGENT_HANDOVER 08-30 | 通用/BOSS/智联/猎聘采集循环在运行（PID 34120/35004、20560/23120、21840/9724、11064/12220 等） | **无任何 python 采集进程，9333 CDP 未监听**；数据停在 jd_pool=128625（signal 最后 captured_at=08-28 21:57） | 文档改为"采集已停止于约 08-28/08-29，待恢复" |
| W2 | 资产与状态 08-25 | signal=1087（github 264/blog 823） | signal=1493（github 354/blog 1139），captured_at 至 08-28 | 更新计数；注明数据截至 08-28 |
| W3 | 资产与状态 08-25 | cross_source=886 | cross_source=885 | 更新计数 |
| W4 | 资产与状态 08-25 | jd_pool 分源 boss 967/liepin 477/zhaopin 622/hn 1794 | boss 1131/liepin 764/zhaopin 1088/hn 1793（linkedin 123849） | 更新分源计数 |
| W5 | A_AGENT_HANDOVER 08-30 增补 | "中英文 title/name_en 已生效" | **已失效**：新 M2 470 条 vs 旧 job_definition_zh.json 22 条，`_load_zh_map` 长度不等跳过映射 → API/图谱岗位名全部回退英文 | 文档标注"中英文过渡已因 M2 新包失效，待 M2 补 job_name_zh 或 A 建映射" |
| W6 | 决策/审计文档 | validate_exchange 为"回包第一关 schema 硬校验、canonical 对齐" | 结构/change_type 枚举为**硬校验**；技能 canonical 为**软校验（警告不阻断，exit=0）**；当前 job_definition 与 job_skill 关联 470/470 已通过 | 文档明确 canonical 当前为软校验、待 M2 修复后转硬 |
| W7 | A_AGENT_HANDOVER | "当前验证服务 18000/18080" | 后端可起 18000 ✓；前端 dev 起 18080 ✓ 但 **vite.config.ts 代理仍指向 8000**（18000 临时配置未提交），/api 实际 ECONNREFUSED → 浏览器联调当前不可用 | 文档注明"需临时改 vite 代理或起 8000 后端" |

另有两处"描述精确化"：
- W8 测试失败根因：6 failed 中 5 个为 M2 新旧语义差异（zh 22 vs 470、中文缺失 0/470、quality reject、canonical 未命中、alias 归一）；1 个采集集成测试确因查询 `WHERE job_title='AI应用工程师'` 未限定 source，取到生产 zhaopin 同名岗位，断言 'Engineering' 不在其中（raw_text 为正常 UTF-8，控制台乱码仅为显示问题）→ 与文档描述相符，建议补一句"取到 zhaopin 同名岗位"。
- W9 图谱契约：graph.json 节点字段是 `type`（job/skill）而非设计文档 §7 的 `kind`（industry/job/skill）；Neo4j 用 label（Job/Skill）保存，无 type 属性；API 转 `kind`；**当前无 industry 节点**。validate_exchange 的 GraphNodeModel 也只认 `type` → 需在文档注明"图谱契约实际实现为 type 字段，与设计文档 kind 表述不一致，待统一"。

---

## 二、五模块真实整合核对（每项均有实测证据）

### 数据链路（真实打通，可复现）
| 链路 | 验证结果 |
|---|---|
| M1→M2 | jd_pool=128625 全 cleaned；`app.job_analysis.pipeline` 可导入；exchange/m2 产出 470 定义+470 技能；import_exchange 全量重建语义与文档一致（DELETE+INSERT 四表） |
| M2→M3 | `app.graph.builder` 今日复跑：**跳过 4426 个未归一技能** → 岗位节点 470 + 技能节点 118、岗位-技能 1017 边、岗位-岗位 2986 边 = **588 节点/4003 边**，与 exchange/m3 一致；import_graph→Neo4j 588/3998（MERGE 去重 5） |
| M2→M4 | matching 实测可用：quick_match 返回 score/match_rate/matched/unmatched；POST /api/resume/upload 返回 profile（技能 canonical 化 docker/python/mysql）+ matchResult（score 40、missing 带 level 但 **tip 全空**、target_job 自动选最高分"Django Engineer"） |
| M2→M5 | GET /api/jobs 真实 470（title 英文）；/api/graph/data 588/3998（kind=job/skill，label 英文）；/api/resume/target-jobs 470 条（label 英文） |
| M4→M5 | 上传链路真实（422 已修），雷达/目标岗位协议未闭环 |
| A 集成层 | validate_exchange / import_exchange / import_graph 全部可运行 |

### 各模块状态（代码 vs 回包 vs 文档）
| 模块 | 结论 |
|---|---|
| M1 | 代码可导入、采集历史数据完好；**进程已停止**（W1）；无关本次前端问题 |
| M2 | 已迁入 backend/app/job_analysis（input 新包全部文件在，另含旧残留 translate.py——git 已跟踪但新 pipeline 不调用，其 prompt 仍含"翻译技能名"逻辑，违反 L1）；**数据质量 4 项未达标**：470/488、job_name_zh=0、canonical 命中 required 107/2739·bonus 65/756·job_skill 111/3269、job_change_log=0 |
| M3 | input 812 节点旧快照未导入（合规）；正式 builder 重建与文档一致；**name_zh 全空、无 industry 节点、RELATED_TO 全为 Job–Job（2986）无 Skill–Skill** |
| M4 | 已迁入 backend/app/matching（input 全部 .py 在，另加 canonical.py/__init__/README）；解析+匹配真实可用；**pathfinder 未实现（tip 空）、target_job 未参与匹配、resume 未落库（0 条）、标注测试集缺失**；真实简历按 D36 保留 |
| M5 | 前端 6 页源码齐、`pnpm run build` 通过；原包 11 路由中 Evolution/Collection/Analytics/GraphCollection/测试页未整合（依赖后端不存在的 /api/evolution、/api/collection、/api/learning、/api/jd-filter）；Learning/ResumeDemo 空态；MagicStick 未导入、AbilityRadar 空 dimensions 崩溃未修（2026-08-30 交接） |
| A 后端 | 20 接口全部存在并实测：jobs/graph/overview/skill-distribution/upload 真实；**trend 空、skill-radar 全 50、industry-tracks 静态、skill-dimensions 空、graph focus/year 未实现、zh 映射失效** |

### 测试（与文档一致）
- 全量 pytest：**223 passed / 6 failed**（与 2026-08-30 报告一致）；失败明细见 W8。
- 前端 build：通过（仅 chunk>500kB 警告）。

---

## 三、待拍板事项（基于上述核对结果）

1. **采集进程恢复**：当前 0 个采集进程、9333 CDP 未监听（文档声称在跑）。是否恢复 BOSS/智联/猎聘低速循环（需 Edge CDP 人工登录页仍在）？还是本轮以"数据已足够（jd_pool 12.8w、job_definition 470）"暂停采集？
2. **文档修正授权**：是否授权按 W1–W9 更新 `资产与状态.md`、`A_AGENT_HANDOVER.md`、README（含采集停止、计数、中英文失效、canonical 软校验、代理未就绪等）？
3. **M2 数据质量归属**：canonical 命中仅约 4%（builder 因此跳过 4426 个技能）、无 job_name_zh、470/488——由 M2 下轮按 skill_dict 重跑并补字段，还是允许 A 侧兜底（建 470 中文映射 + alias 归一）？
4. **中英文短期方案**：等 M2 补 job_name_zh，还是 A 先建 470 条中文展示映射恢复 API 中文名（不落库，仅展示层）？
5. **页面集**：6 页 vs 恢复 Evolution（竞赛需求 2）/Collection/Analytics 共 9 页？
6. **Learning/pathfinder**：等 M4 实现，还是 A 先做最小 pathfinder（missing→level/tip）让 Learning 页有真实数据？
7. **前端联调**：临时改 vite 代理到 18000（不提交）以完成浏览器验收，还是起 8000 后端？

---

## 四、用户裁决与执行记录（2026-08-31）

| # | 原待拍板项 | 用户裁决 | 执行状态 |
|---|---|---|---|
| 1 | 采集进程恢复 | 数据量已够、暂不采集；循环状态由接手 agent 自行检查 | 已写入 README / 资产与状态 / A_AGENT_HANDOVER |
| 2 | 文档修正授权 | 可以 | W1–W9 已修正：README、`docs/superpowers/资产与状态.md`、`A_AGENT_HANDOVER.md`、`CLAUDE.md`、`决策跟踪.md`（P5 关闭 + 08-31 已落地） |
| 3 | M2 数据质量归属 | M2 后续补发翻译后岗位数据；相关任务适当调整、收到后再完成 | 已记录；中文名落库/图谱 name_zh 重建任务挂起等 M2 新包 |
| 4 | 中英文短期方案 | 待用户选（方案 A：等 M2 包一次性闭环；方案 B：A 侧先建 470 条展示映射立即恢复中文） | 见回复 |
| 5 | 页面集 | 按当前 frontend/ 6 页实际设计 | 维持不变 |
| 6 | Learning/pathfinder | M4 正在实现、稍后回包；任务适当调整 | 已记录；pathfinder/Learning 接入任务挂起等 M4 新回包 |
| 7 | 前端联调端口 | 选不易占用、方便使用的端口，功能优先 | 定案后端 18000 / 前端 18080；`frontend/vite.config.ts` 已改为 dev 端口 18080、代理默认指向 18000（`VITE_PROXY_TARGET` 可覆盖）；`pnpm run build` 通过 |
