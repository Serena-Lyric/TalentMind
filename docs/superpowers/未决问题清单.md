# TalentMind 未决问题清单（集中跟踪）

> 整理日期：2026-09-03。本清单集中收纳“尚未解决 / 待办”事项及其跟踪位置；已解决事项不在此列（见 `决策跟踪.md` D 表与 traps）。每条带跟踪指针，避免问题散落在多处无人跟进。

## 决策跟踪 P 表未决项（权威：`docs/superpowers/决策跟踪.md`）

| 编号 | 事项 | 状态 |
|---|---|---|
| P1 | ≥100 条 JD 标注测试集 + 三项准确率 ≥90% 验证（含 M2 is_emerging 复核） | 建议 M2+M4 构建、A 验收，待定稿 |
| P2 | 里程碑节奏细化 | 4 周粗排已定 |
| P3 | 部署说明与演示材料（docker-compose 部署说明、演示视频） | 待补 |
| P6 | 中文平台（拉勾/智联/猎聘）合规数据获取方案 | BOSS 已可控 CDP；其余待合规方案 |

> P4（change_type 列宽）与 P5（job_skill↔job_definition 关联）已关闭，不在此列。

## 开发/模块待办（跟踪位置：`资产与状态.md` 已知限制 / `A_AGENT_HANDOVER.md` 未完成事项）

1. **M4 pathfinder 未回包**：学习路径已于 2026-09-04 升级为数据驱动实现——按「基础→核心→专项→加分」分层分步生成路径，逐技能给出含 JD 证据与简历同类别衔接的针对性建议（见 `docs/superpowers/specs/2026-09-04-learning-path-improvement-design.md`）；M4 pathfinder 若回包仍可接入其路径/依赖建议算法（`资产与状态.md` M4 待办）。
2. **resume 落库待定**：`POST /api/resume/upload` 不落库，dashboard totalResumes 恒 0（`资产与状态.md` 已知限制 4）。
3. **M3 Skill–Skill 关系**：当前图谱暂无技能-技能关系（`A_AGENT_HANDOVER.md` 未完成事项）。
4. **`extract_skills` 中文紧邻英文提取缺陷**：原型 `\b` 边界问题，测试已规避，待优化（`资产与状态.md` 已知限制 9）。
5. **阶段 7 清理（待用户确认）**：`input/` 对照包（`图谱模块/`、`岗位能力图谱前端系统/`、`人岗匹配/`）删除前需先列清单与用户确认（D36，真实简历不迁移不入库）。
6. **前端 chunk >500kB**：体积优化，非阻塞（`资产与状态.md` 已知限制 10）。
7. **后端模型/注释遗留清理**：`models/schemas.py::EmergingJobOut`、`main.py` “计划 A~E”注释待清理（`资产与状态.md` 已知限制 14）。

## 数据/运维待复核

1. **3 条岗位级变化日志无法关联内部岗位 ID**：保留原始 `m2_job_id`（`job_id=NULL`）作审计，后续导入新回包时复核（`决策跟踪.md` P5 已关闭记录、`资产与状态.md` 顶部）。
2. **历史 GitHub 已含的大文件**：3. **每日采集任务与 D55 冲突（2026-09-04）**：计划任务 `TalentMindCollect` 每日 08:01 运行 `fetch_all`（含 HN 段），会把当月 HN 原始 JD 重新写入 jd_pool 并覆盖 `exchange/m1/cross_validate_report.md`，与 D55「HN/LinkedIn 原始 JD 不入主库」冲突；已按用户要求加暂停开关 `data/local/PAUSE_COLLECT`（`scripts/collect_daily.ps1` 检测到即退出）。长期方案（任务命令改 `--skip-hn` / 直接暂停，需管理员）待用户裁决（2026-09-04）。
`docs/originalfile/` 曾入库约 40MB+ 数据文件，D57 后不再同步新内容；如需彻底从远端历史清除需重写历史（默认不做，另行评估）。

## 维护规则

- 新发现问题先归到上面对应条目，找不到归属再新增；解决后从本清单移除并在 `决策跟踪.md` / traps 留痕。
- 本清单与 `决策跟踪.md` P 表、`资产与状态.md` 已知限制冲突时，以后两者为准并同步本清单。
